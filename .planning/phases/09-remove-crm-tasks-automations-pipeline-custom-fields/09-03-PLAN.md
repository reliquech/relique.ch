---
phase: 09-remove-crm-tasks-automations-pipeline-custom-fields
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/admin/crm/**
  - src/app/api/pipeline-stages/**
  - src/app/api/custom-fields/**
  - src/app/api/custom-field-values/**
  - src/app/admin/(portal)/pipeline-stages/**
  - src/app/admin/(portal)/custom-fields/**
  - src/components/admin/PortalSidebar.tsx
  - src/lib/utils/admin.tsx
  - src/admin/crm/schemas.ts
  - src/lib/types/admin.ts
autonomous: true
requirements:
  - PRUNE-09-04
  - PRUNE-09-05
must_haves:
  truths:
    - "Deals page works list-only — no kanban, no pipeline stage filter"
    - "CRM create/edit flows work without custom_fields or pipeline_stage_id in payloads"
    - "Pipeline Stages and Custom Fields routes removed from nav"
  artifacts:
    - path: src/admin/crm/pages/DealsPage.tsx
      provides: "Simplified deals list without pipeline/custom-fields deps"
---

<objective>
Remove Pipeline Stages and Custom Fields admin features; decouple Deals + CRM forms so core CRM (customers, leads, deals, messages) still works.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-remove-crm-tasks-automations-pipeline-custom-fields/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Pipeline + Custom Fields admin surfaces</name>
  <action>
Delete files:
- `src/app/admin/(portal)/pipeline-stages/page.tsx`
- `src/app/admin/(portal)/custom-fields/page.tsx`
- `src/app/api/pipeline-stages/**`
- `src/app/api/custom-fields/**`
- `src/app/api/custom-field-values/route.ts`
- `src/admin/crm/pages/PipelineStagesPage.tsx`
- `src/admin/crm/pages/CustomFieldsPage.tsx`
- `src/admin/crm/components/PipelineStagesList.tsx`
- `src/admin/crm/components/PipelineStageForm.tsx`
- `src/admin/crm/components/CustomFieldForm.tsx`
- `src/admin/crm/components/CustomFieldsSection.tsx`
- `src/admin/crm/components/DealsBoard.tsx`
- `src/admin/crm/services/pipelineStagesService.ts`
- `src/admin/crm/services/customFieldsService.ts`
- `src/admin/crm/services/customFieldValuesService.ts`

Remove exports from `src/admin/crm/index.ts`.
Remove sidebar items + tab maps for pipeline-stages, custom-fields.
  </action>
</task>

<task type="auto">
  <name>Task 2: Simplify DealsPage</name>
  <files>src/admin/crm/pages/DealsPage.tsx</files>
  <action>
- Remove kanban view mode, `DealsBoard`, `fetchKanban`, stage filter UI
- Remove imports: pipelineStagesService, customFieldsService, customFieldValuesService
- Remove `custom_fields` / `customFieldValues` handling in save handlers
- Remove `pipeline_stage_id` from form defaultValues and `handleDealMove`
- Keep list view + DealDetail + create/edit dialog with status (open/won/lost) only
- Default `viewMode` to list only (delete view toggle if only one mode)
  </action>
  <verify>DealsPage compiles; no pipeline/custom-field service imports</verify>
</task>

<task type="auto">
  <name>Task 3: Simplify CRM forms</name>
  <files>
    src/admin/crm/components/DealForm.tsx
    src/admin/crm/components/DealDetail.tsx
    src/admin/crm/components/LeadForm.tsx
    src/admin/crm/components/CustomerForm.tsx
    src/admin/crm/components/MessageDetail.tsx
    src/admin/crm/components/LeadConvertDialog.tsx
    src/admin/crm/pages/LeadsPage.tsx
    src/admin/crm/pages/CustomersPage.tsx
    src/admin/crm/schemas.ts
  </files>
  <action>
- Remove `CustomFieldsSection`, custom_fields from schemas (DealSchema, LeadSchema, etc.)
- Remove `stages` / `pipeline_stage_id` fields from DealForm and LeadConvertDialog
- LeadConvertDialog: create deal without pipeline_stage_id
- LeadsPage/CustomersPage: remove customFieldValuesService fetch/upsert
- DealDetail: remove custom fields panel and stage props
- Update API payloads to omit `pipeline_stage_id` and custom field values (server routes still accept until plan 04 DB migration — make optional/nullable)
  </action>
</task>

<task type="auto">
  <name>Task 4: Types cleanup</name>
  <files>src/lib/types/admin.ts</files>
  <action>
Remove or deprecate `PipelineStage`, `CustomField` types if unused; fix all imports.
  </action>
  <verify>npm run check-types passes; phase9 grep patterns for pipeline/custom-fields = 0</verify>
</task>

</tasks>

<verification>
- [ ] Create customer, lead, deal from admin UI (manual smoke note in SUMMARY)
- [ ] npm run check-types passes
- [ ] npm run phase9:grep-gate passes for pipeline + custom-fields patterns (may still fail tasks until plan 02 merged — run full gate after wave 2)
</verification>

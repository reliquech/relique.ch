---
phase: 09-remove-crm-tasks-automations-pipeline-custom-fields
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase9-grep-gate.mjs
  - .planning/phases/09-remove-crm-tasks-automations-pipeline-custom-fields/09-INVENTORY.md
  - package.json
autonomous: true
requirements:
  - PRUNE-09-01
must_haves:
  truths:
    - "09-INVENTORY.md lists every file/route/table to delete with evidence paths"
    - "phase9:grep-baseline captures pre-removal ref counts for regression compare"
  artifacts:
    - path: scripts/phase9-grep-gate.mjs
      provides: "Forbidden-pattern grep gate for removed modules"
    - path: .planning/phases/09-remove-crm-tasks-automations-pipeline-custom-fields/09-INVENTORY.md
      provides: "Deletion checklist"
---

<objective>
Baseline inventory and automated grep gate before deleting Tasks, Automations, Pipeline Stages, Custom Fields.

Output: `09-INVENTORY.md` + `scripts/phase9-grep-gate.mjs` + `npm run phase9:grep-baseline`
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-remove-crm-tasks-automations-pipeline-custom-fields/09-CONTEXT.md
@.planning/phases/09-remove-crm-tasks-automations-pipeline-custom-fields/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 09-INVENTORY.md</name>
  <action>
Document exhaustive delete list grouped by module:

**Routes:** `src/app/admin/(portal)/{tasks,automations,pipeline-stages,custom-fields}/`
**API:** `tasks`, `alert-rules`, `pipeline-stages`, `custom-fields`, `custom-field-values`
**Modules:** `src/admin/tasks/**`, `src/admin/automations/**`
**CRM files:** PipelineStagesPage, PipelineStagesList, PipelineStageForm, CustomFieldsPage, CustomFieldForm, CustomFieldsSection, pipelineStagesService, customFieldsService, customFieldValuesService, DealsBoard (kanban-only)
**Wiring:** PortalSidebar items, admin.tsx maps, AdminPortalLayout AlertScheduler, DashboardPage TasksWidget
**Cross-refs:** DealsPage, DealForm, DealDetail, LeadForm, CustomerForm, MessageDetail, LeadConvertDialog, LeadsPage, CustomersPage, crm/index.ts exports, api/activity tasks branch
**DB:** tasks, alert_rules, pipeline_stages, crm_custom_fields, crm_custom_field_values, deals.pipeline_stage_id, crm_stage_velocity RPC

Include line counts / file paths from grep evidence.
  </action>
  <verify>File exists with 4 module sections + DB section</verify>
</task>

<task type="auto">
  <name>Task 2: phase9-grep-gate script</name>
  <files>scripts/phase9-grep-gate.mjs, package.json</files>
  <action>
Create `scripts/phase9-grep-gate.mjs` (Node ESM) that scans `src/**/*.{ts,tsx}` for forbidden patterns:

```
/api/tasks
admin/tasks
TasksWidget
tasksService
/api/alert-rules
admin/automations
AlertScheduler
alertRulesService
/api/pipeline-stages
PipelineStagesPage
pipelineStagesService
DealsBoard
/api/custom-fields
custom-field-values
CustomFieldsSection
customFieldsService
customFieldValuesService
```

Modes:
- `--baseline` — print counts per pattern (for 09-01 snapshot)
- default — exit 1 if any match > 0

Add scripts:
- `"phase9:grep-baseline": "node scripts/phase9-grep-gate.mjs --baseline"`
- `"phase9:grep-gate": "node scripts/phase9-grep-gate.mjs"`

Run baseline now and append counts to 09-INVENTORY.md.
  </action>
  <verify>npm run phase9:grep-baseline exits 0 with non-zero counts</verify>
</task>

</tasks>

<verification>
- [ ] 09-INVENTORY.md complete
- [ ] npm run phase9:grep-baseline runs
</verification>

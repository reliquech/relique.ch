---
phase: 09-remove-crm-tasks-automations-pipeline-custom-fields
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/admin/tasks/**
  - src/admin/automations/**
  - src/app/api/tasks/**
  - src/app/api/alert-rules/**
  - src/app/admin/(portal)/tasks/**
  - src/app/admin/(portal)/automations/**
  - src/components/admin/AdminPortalLayout.tsx
  - src/admin/dashboard/pages/DashboardPage.tsx
  - src/app/api/activity/route.ts
  - src/components/admin/PortalSidebar.tsx
  - src/lib/utils/admin.tsx
autonomous: true
requirements:
  - PRUNE-09-02
  - PRUNE-09-03
must_haves:
  truths:
    - "Tasks and Automations admin routes return 404"
    - "No TasksWidget or AlertScheduler mounted in admin shell"
    - "api/activity no longer queries tasks table"
  artifacts:
    - path: scripts/phase9-grep-gate.mjs
      provides: "Zero matches for tasks + automations patterns"
---

<objective>
Remove Tasks and Automations modules entirely from app layer (UI, API, nav, background schedulers).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-remove-crm-tasks-automations-pipeline-custom-fields/09-INVENTORY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Tasks module</name>
  <action>
Delete:
- `src/admin/tasks/**`
- `src/app/api/tasks/**`
- `src/app/admin/(portal)/tasks/page.tsx`

Remove `TasksWidget` import/usage from `src/admin/dashboard/pages/DashboardPage.tsx`.
Remove tasks branch from `src/app/api/activity/route.ts` (stop querying `.from("tasks")`; remove TaskRow type usage if orphaned).
  </action>
  <verify>rg "admin/tasks|TasksWidget|/api/tasks" src/ returns 0</verify>
</task>

<task type="auto">
  <name>Task 2: Delete Automations module</name>
  <action>
Delete:
- `src/admin/automations/**`
- `src/app/api/alert-rules/**`
- `src/app/admin/(portal)/automations/page.tsx`

Remove `AlertScheduler` from `src/components/admin/AdminPortalLayout.tsx`.
  </action>
  <verify>rg "alert-rules|AlertScheduler|admin/automations" src/ returns 0</verify>
</task>

<task type="auto">
  <name>Task 3: Nav + tab maps</name>
  <files>src/components/admin/PortalSidebar.tsx, src/lib/utils/admin.tsx</files>
  <action>
Remove sidebar items: tasks, automations.
Remove from `routeToTabMap`, `tabToRouteMap`, `tabNames` in admin.tsx.
  </action>
  <verify>Sidebar CRM group has only customers, leads, deals (+ admin custom-fields if still present — plan 03 removes)</verify>
</task>

<task type="auto">
  <name>Task 4: Typecheck slice</name>
  <action>Run `npm run check-types`; fix broken imports from deletions.</action>
  <verify>check-types passes for tasks/automations grep zero</verify>
</task>

</tasks>

<verification>
- [ ] rg tasks/automations forbidden patterns = 0
- [ ] npm run check-types passes
</verification>

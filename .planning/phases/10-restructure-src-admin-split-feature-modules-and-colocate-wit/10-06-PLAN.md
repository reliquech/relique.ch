---
phase: 10-restructure-src-admin-split-feature-modules-and-colocate-wit
plan: 06
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/features/crm/**
  - src/components/admin/crm/**
  - src/components/shared/CrmViewBar.tsx
  - src/app/admin/(portal)/leads/page.tsx
  - src/app/admin/(portal)/deals/page.tsx
  - src/app/admin/(portal)/customers/page.tsx
  - src/app/admin/(portal)/messages/page.tsx
autonomous: true
requirements:
  - RESTR-10-02
  - RESTR-10-05
must_haves:
  truths:
    - "CrmViewBar at components/admin/crm/CrmViewBar.tsx"
    - "LeadsPage and CustomersPage each ≤300 lines"
    - "All CRM services in features/crm/services"
  artifacts:
    - path: src/components/admin/crm/CrmViewBar.tsx
      provides: "CRM saved views bar (moved from shared)"
    - path: src/features/crm/schemas.ts
      provides: "CRM Zod schemas"
---

<objective>
Wave 5: Migrate CRM domain (largest), move CrmViewBar, split oversized list pages.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/10-restructure-src-admin-split-feature-modules-and-colocate-wit/10-INVENTORY.md
@.planning/phases/10-restructure-src-admin-split-feature-modules-and-colocate-wit/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move CRM features layer</name>
  <action>
Move to `src/features/crm/`:
- `services/*` (10 files from admin/crm/services)
- `hooks/useAttachmentsUpload.ts`
- `schemas.ts` (from admin)
- Keep `types.ts` — delete `src/admin/crm/types.ts` duplicate

Update `src/app/api/activity/route.ts` and any other API imports if still on admin paths.

Optional: `src/features/crm/index.ts` barrel for services.
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <done>CRM services/hooks/schemas in features/crm</done>
</task>

<task type="auto">
  <name>Task 2: Move CRM components + pages</name>
  <action>
- `components/*` → `src/components/admin/crm/components/`
- `pages/*` → `src/components/admin/crm/pages/`

Move `src/components/shared/CrmViewBar.tsx` → `src/components/admin/crm/CrmViewBar.tsx`
Update imports to `@/features/crm/services/*`, `@/features/users/hooks/useProfile`.

Delete old CrmViewBar from shared/ (no re-export shim).

Update 4 CRM page imports from `@/components/shared/CrmViewBar` to `@/components/admin/crm/CrmViewBar`.
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <done>CrmViewBar in components/admin/crm/; CRM UI in components/admin/crm/</done>
</task>

<task type="auto">
  <name>Task 3: Split oversized pages</name>
  <action>
**LeadsPage** (355 lines) — extract:
- `LeadsTableColumns.tsx` or useMemo columns hook file
- `LeadsBulkActions.tsx` (toolbar when rows selected)
- Keep page orchestration ≤300 lines

**CustomersPage** (346 lines) — same extraction pattern.

**DealsPage** — verify already ≤300; split if needed.

All new files ≤300 lines.
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <done>LeadsPage, CustomersPage, DealsPage each ≤300 lines</done>
</task>

<task type="auto">
  <name>Task 4: Update routes + gate</name>
  <action>
Update thin re-exports: leads, deals, customers, messages.

Run `npm run check-types`.
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <done>CRM routes updated; zero @/admin/crm refs</done>
</task>

</tasks>

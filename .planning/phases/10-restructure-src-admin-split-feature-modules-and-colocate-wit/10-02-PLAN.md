---
phase: 10-restructure-src-admin-split-feature-modules-and-colocate-wit
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/features/users/**
  - src/features/notifications/**
  - src/components/admin/users/**
  - src/components/admin/notifications/**
  - src/components/admin/shell/**
  - src/app/admin/login/page.tsx
  - src/app/admin/(portal)/users/page.tsx
  - src/app/admin/(portal)/profile/page.tsx
autonomous: true
requirements:
  - RESTR-10-02
must_haves:
  truths:
    - "users + notifications modules have zero @/admin/users or @/admin/notifications imports from new paths"
    - "useProfile imported from @/features/users/hooks/useProfile everywhere migrated in this wave"
    - "ProfilePage single source in components/admin/users/pages"
  artifacts:
    - path: src/features/users/hooks/useProfile.ts
      provides: "Shared profile hook"
    - path: src/components/admin/shell/PortalSidebar.tsx
      provides: "Shell moved from components/admin root"
---

<objective>
Wave 1: Migrate users + notifications + admin shell. Foundation for all CRM pages (useProfile fan-out).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/10-restructure-src-admin-split-feature-modules-and-colocate-wit/10-INVENTORY.md
@.planning/phases/10-restructure-src-admin-split-feature-modules-and-colocate-wit/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move users domain</name>
  <action>
`git mv` (or equivalent) from `src/admin/users/`:

**features:**
- `services/usersService.ts` → `src/features/users/services/`
- `hooks/useProfile.ts` → `src/features/users/hooks/`
- `types.ts` → `src/features/users/types.ts`

**components:**
- `pages/LoginPage.tsx`, `UsersPage.tsx`, `ProfilePage.tsx`, `SettingsPage.tsx` → `src/components/admin/users/pages/`

Update imports in moved files to `@/features/users/*`.

Consolidate `src/app/admin/(portal)/profile/page.tsx` — replace inline implementation with thin re-export from `components/admin/users/pages/ProfilePage`.

Update `src/app/admin/login/page.tsx` and `users/page.tsx` re-export paths.
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <done>users domain in features/ + components/admin/users; profile page consolidated</done>
</task>

<task type="auto">
  <name>Task 2: Move notifications domain</name>
  <action>
- `src/admin/notifications/components/*` → `src/components/admin/notifications/components/`
- `src/admin/notifications/services/*` → `src/features/notifications/services/`
- `types.ts` → `src/features/notifications/types.ts`

Update `AdminPortalLayout.tsx` NotificationCenter import.
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <done>notifications in features/ + components/admin/notifications; zero @/admin/notifications refs</done>
</task>

<task type="auto">
  <name>Task 3: Reorganize shell</name>
  <action>
Move to `src/components/admin/shell/`:
- `PortalSidebar.tsx`
- `AdminPortalLayout.tsx`

Update layout imports in `src/app/admin/(portal)/layout.tsx`.

Update shell imports: `useProfile` from `@/features/users/hooks/useProfile`.
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <done>shell in components/admin/shell/; layout imports updated</done>
</task>

</tasks>

<verification>
- [ ] `@/admin/users` and `@/admin/notifications` eliminated from migrated files
- [ ] Profile page not duplicated
- [ ] check-types PASS
</verification>

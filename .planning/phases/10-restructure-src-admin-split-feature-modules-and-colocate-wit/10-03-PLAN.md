---
phase: 10-restructure-src-admin-split-feature-modules-and-colocate-wit
plan: 03
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - src/features/dashboard/**
  - src/components/admin/dashboard/**
  - src/app/admin/(portal)/page.tsx
  - src/app/admin/(portal)/logs/page.tsx
autonomous: true
requirements:
  - RESTR-10-02
must_haves:
  truths:
    - "Dashboard + logs pages import from components/admin/dashboard"
    - "dashboard services live in features/dashboard"
  artifacts:
    - path: src/components/admin/dashboard/pages/DashboardPage.tsx
      provides: "Admin home dashboard"
---

<objective>
Wave 2: Migrate dashboard domain (pages, components, services).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/10-restructure-src-admin-split-feature-modules-and-colocate-wit/10-INVENTORY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move dashboard domain</name>
  <action>
**features:**
- `src/admin/dashboard/services/*` → `src/features/dashboard/services/`
- `types.ts` → `src/features/dashboard/types.ts`

**components:**
- `pages/DashboardPage.tsx`, `LogsPage.tsx` → `src/components/admin/dashboard/pages/`
- `components/*` → `src/components/admin/dashboard/components/`

Update internal imports to `@/features/dashboard/*` and `@/features/users/hooks/useProfile` where needed.

Update app routes:
- `src/app/admin/(portal)/page.tsx` → re-export DashboardPage
- `src/app/admin/(portal)/logs/page.tsx` → re-export LogsPage
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <done>dashboard domain migrated; zero @/admin/dashboard refs</done>
</task>

<task type="auto">
  <name>Task 2: Wave gate</name>
  <action>Run `npm run check-types`. Fix any broken imports.</action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <done>check-types PASS after dashboard wave</done>
</task>

</tasks>

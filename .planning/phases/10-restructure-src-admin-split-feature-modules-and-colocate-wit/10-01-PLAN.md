---
phase: 10-restructure-src-admin-split-feature-modules-and-colocate-wit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase10-grep-gate.mjs
  - package.json
  - .planning/phases/10-restructure-src-admin-split-feature-modules-and-colocate-wit/10-INVENTORY.md
  - src/lib/hooks/useDebounce.ts
  - src/hooks/useDebounce.ts
  - src/components/forms/ConsignForm.tsx
  - src/components/filters/MarketplaceSearch.tsx
autonomous: true
requirements:
  - RESTR-10-01
  - RESTR-10-05
must_haves:
  truths:
    - "10-INVENTORY.md maps every src/admin file to target path"
    - "phase10:grep-baseline captures @/admin ref counts"
    - "Single useDebounce at @/hooks/useDebounce"
  artifacts:
    - path: scripts/phase10-grep-gate.mjs
      provides: "Forbidden @/admin/ grep gate"
    - path: .planning/phases/10-restructure-src-admin-split-feature-modules-and-colocate-wit/10-INVENTORY.md
      provides: "Move map for all 82 files"
---

<objective>
Wave 0 prep: inventory, grep gate baseline, useDebounce consolidation, file-split checklist for oversized components.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/10-restructure-src-admin-split-feature-modules-and-colocate-wit/10-CONTEXT.md
@.planning/phases/10-restructure-src-admin-split-feature-modules-and-colocate-wit/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 10-INVENTORY.md</name>
  <files>.planning/phases/10-restructure-src-admin-split-feature-modules-and-colocate-wit/10-INVENTORY.md</files>
  <action>
Create move map grouped by domain (users, notifications, dashboard, submissions, marketplace, crm, shell).

For each file under `src/admin/**` document:
- Current path
- Target path (`components/admin/...` or `features/...`)
- Import dependents (grep `@/admin/...` refs)

Include split plan for:
- MarketplaceForm (478) → orchestrator + existing form sections
- LeadsPage (355) → extract columns, bulk toolbar, form dialog body
- CustomersPage (346) → same pattern as LeadsPage
  </action>
  <verify>
    <automated>test -f .planning/phases/10-restructure-src-admin-split-feature-modules-and-colocate-wit/10-INVENTORY.md</automated>
  </verify>
  <done>10-INVENTORY.md lists all 6 domains + shell with target paths and split checklist</done>
</task>

<task type="auto">
  <name>Task 2: phase10-grep-gate script</name>
  <files>scripts/phase10-grep-gate.mjs, package.json</files>
  <action>
Create `scripts/phase10-grep-gate.mjs` modeled on phase9:
- Patterns: `@/admin/`, `from "@/admin/`, `from '@/admin/`
- `--baseline`: print file counts per pattern
- default: exit 1 if any match in `src/**/*.{ts,tsx}`
- Optional: fail if `src/admin/` directory still exists

Add scripts:
```json
"phase10:grep-baseline": "node scripts/phase10-grep-gate.mjs --baseline",
"phase10:grep-gate": "node scripts/phase10-grep-gate.mjs",
"phase10:gate": "npm run phase10:grep-gate && npm run check-types && npm run build"
```

Run `npm run phase10:grep-baseline` and record counts in 10-INVENTORY.md.
  </action>
  <verify>
    <automated>npm run phase10:grep-baseline</automated>
  </verify>
  <done>phase10 scripts in package.json; baseline counts recorded in 10-INVENTORY.md</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate useDebounce</name>
  <files>src/hooks/useDebounce.ts, src/lib/hooks/useDebounce.ts, src/components/forms/ConsignForm.tsx, src/components/filters/MarketplaceSearch.tsx</files>
  <action>
- Ensure `src/hooks/useDebounce.ts` is canonical (keep existing)
- Update `ConsignForm.tsx` and `MarketplaceSearch.tsx` to import from `@/hooks/useDebounce`
- Delete `src/lib/hooks/useDebounce.ts` if no remaining importers
- Grep confirms single `export function useDebounce`
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <done>Single useDebounce at @/hooks/useDebounce; lib/hooks duplicate deleted</done>
</task>

</tasks>

<verification>
- [ ] 10-INVENTORY.md complete
- [ ] phase10:grep-baseline recorded
- [ ] useDebounce single source
</verification>

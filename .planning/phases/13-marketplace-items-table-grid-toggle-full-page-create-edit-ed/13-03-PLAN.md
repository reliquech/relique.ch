---
phase: 13-marketplace-items-table-grid-toggle-full-page-create-edit-ed
plan: 03
type: execute
wave: 3
depends_on:
  - 13-02
files_modified:
  - src/features/marketplace/hooks/useOnlineStatus.ts
  - src/components/admin/marketplace/items/MarketplaceTypedDeleteDialog.tsx
  - src/components/admin/marketplace/items/MarketplaceItemsBulkBar.tsx
  - src/components/admin/marketplace/items/MarketplaceItemRowMenu.tsx
  - src/components/admin/marketplace/pages/ItemsPage.tsx
autonomous: true
requirements:
  - ITEMS-13-05
must_haves:
  truths:
    - "Offline state appears on list when navigator reports offline"
    - "Viewer role cannot access mutating list actions"
    - "Permanent delete uses typed confirmation"
    - "Bulk delete requires DELETE for multi-item delete"
---

<objective>
Add Marketplace Items list UX polish for offline state, permission-sensitive actions, and typed destructive confirmation.
</objective>

<threat_model>
T13-04: Viewer users can trigger mutations if controls are hidden visually but actions still execute. Mutating callbacks must be gated by `canEdit` before calling service methods.
T13-05: Permanent delete can be triggered accidentally. Require exact item title for single delete and `DELETE` for multi-delete before enabling confirmation.
</threat_model>

<tasks>
<task id="13-03-01" type="auto">
  <name>Create online status hook</name>
  <files>src/features/marketplace/hooks/useOnlineStatus.ts</files>
  <read_first>
    - src/features/marketplace/hooks/useMarketplaceItemsQuery.ts
    - .planning/phases/13-marketplace-items-table-grid-toggle-full-page-create-edit-ed/13-UI-SPEC.md
  </read_first>
  <action>
Create `useOnlineStatus.ts` as a client hook.
Return `true` during SSR and before mount.
On mount, set state from `navigator.onLine`.
Subscribe to `online` and `offline` window events.
Cleanup both event listeners.
  </action>
  <acceptance_criteria>
    - `useOnlineStatus.ts` contains `navigator.onLine`
    - `useOnlineStatus.ts` contains `addEventListener("online"`
    - `useOnlineStatus.ts` contains `addEventListener("offline"`
    - `npx eslint --max-warnings 0 src/features/marketplace/hooks/useOnlineStatus.ts` exits 0
  </acceptance_criteria>
</task>

<task id="13-03-02" type="auto">
  <name>Create typed delete dialog</name>
  <files>src/components/admin/marketplace/items/MarketplaceTypedDeleteDialog.tsx</files>
  <read_first>
    - src/components/shared/DeleteConfirmModal.tsx
    - src/components/ui/dialog.tsx
    - src/components/ui/button.tsx
    - .planning/phases/13-marketplace-items-table-grid-toggle-full-page-create-edit-ed/13-UI-SPEC.md
  </read_first>
  <action>
Create `MarketplaceTypedDeleteDialog.tsx` using existing shadcn dialog/button/input primitives if available.
Props:
`open`, `onOpenChange`, `items: Array<{ id: string; title: string }>`, `onConfirm`, `isDeleting?: boolean`.
If `items.length === 1`, required phrase is the item title. Body copy: `Type "{item title}" to confirm. This cannot be undone.`
If `items.length > 1`, required phrase is `DELETE`. Body copy: `Type "DELETE" to permanently delete {n} items. This cannot be undone.`
Disable confirm until typed value exactly matches required phrase.
Confirm button label: `Delete permanently`.
  </action>
  <acceptance_criteria>
    - `MarketplaceTypedDeleteDialog.tsx` contains `Delete permanently`
    - `MarketplaceTypedDeleteDialog.tsx` contains `DELETE`
    - `MarketplaceTypedDeleteDialog.tsx` contains `This cannot be undone`
    - `MarketplaceTypedDeleteDialog.tsx` contains `disabled=`
    - `npx eslint --max-warnings 0 src/components/admin/marketplace/items/MarketplaceTypedDeleteDialog.tsx` exits 0
  </acceptance_criteria>
</task>

<task id="13-03-03" type="auto">
  <name>Gate mutating list actions by role/offline state</name>
  <files>
    src/components/admin/marketplace/items/MarketplaceItemsBulkBar.tsx
    src/components/admin/marketplace/items/MarketplaceItemRowMenu.tsx
    src/components/admin/marketplace/pages/ItemsPage.tsx
  </files>
  <read_first>
    - src/components/admin/marketplace/items/MarketplaceItemsBulkBar.tsx
    - src/components/admin/marketplace/items/MarketplaceItemRowMenu.tsx
    - src/components/admin/marketplace/pages/ItemsPage.tsx
    - src/features/users/hooks/useProfile.ts
    - src/features/marketplace/hooks/useOnlineStatus.ts
  </read_first>
  <action>
In `ItemsPage`, call `useOnlineStatus()`.
Render an offline banner above the toolbar when offline with text `You are offline. Showing the last loaded items if available.`
Pass `canEdit && online` to bulk/action components.
Before `runBulk`, `updateStatus`, `duplicate`, or delete confirmation paths, return early with toast `You do not have permission to edit marketplace items` when `!canEdit`, and toast `You are offline. Reconnect before changing marketplace items.` when `!online`.
Update row menu so mutating items are hidden or disabled when `canEdit` is false; Preview remains available when slug exists.
Replace generic delete confirmation in `ItemsPage` with `MarketplaceTypedDeleteDialog`.
  </action>
  <acceptance_criteria>
    - `ItemsPage.tsx` contains `useOnlineStatus`
    - `ItemsPage.tsx` contains `You are offline. Showing the last loaded items if available.`
    - `ItemsPage.tsx` contains `MarketplaceTypedDeleteDialog`
    - `ItemsPage.tsx` contains `You do not have permission to edit marketplace items`
    - `MarketplaceItemRowMenu.tsx` contains `canEdit`
    - `npx eslint --max-warnings 0 src/components/admin/marketplace/items/MarketplaceItemsBulkBar.tsx src/components/admin/marketplace/items/MarketplaceItemRowMenu.tsx src/components/admin/marketplace/pages/ItemsPage.tsx` exits 0
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run targeted lint. Manual UAT must confirm viewer role cannot mutate and typed delete blocks until the exact phrase is entered.
</verification>

<success_criteria>
- Offline state is explicit.
- Mutating actions are role- and online-gated in UI and callback code.
- Typed delete protects single and bulk permanent deletion.
</success_criteria>


---
phase: 13-marketplace-items-table-grid-toggle-full-page-create-edit-ed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/marketplace/hooks/useMarketplaceItemsView.ts
  - src/components/admin/marketplace/items/MarketplaceItemsViewToggle.tsx
  - src/components/admin/marketplace/items/MarketplaceItemsCountSummary.tsx
  - src/components/admin/marketplace/items/MarketplaceItemsToolbar.tsx
  - src/components/admin/marketplace/pages/ItemsPage.tsx
autonomous: true
requirements:
  - ITEMS-13-01
must_haves:
  truths:
    - "Table/Grid toggle exists in the items toolbar"
    - "View preference persists with localStorage key relique.admin.marketplace.itemsView"
    - "Mobile without saved preference defaults to Grid"
    - "Count summary displays visible/filtered/total item counts"
---

<objective>
Add Marketplace Items Table/Grid view state, persisted preference, toolbar toggle, and count summary without changing Phase 12 filter/query contracts.
</objective>

<threat_model>
T13-01: Browser storage can be absent, blocked, or corrupted. The hook must guard `typeof window === "undefined"`, catch localStorage errors, ignore invalid values, and default safely.
T13-02: View switching can drop selected IDs, URL filters, sort, or pagination. Keep all query and selection state in `ItemsPage`; the view state must only choose which body component renders.
</threat_model>

<tasks>
<task id="13-01-01" type="auto">
  <name>Create view preference hook</name>
  <files>src/features/marketplace/hooks/useMarketplaceItemsView.ts</files>
  <read_first>
    - src/features/marketplace/hooks/useMarketplaceItemsUrl.ts
    - src/features/marketplace/types/itemsList.ts
    - .planning/phases/13-marketplace-items-table-grid-toggle-full-page-create-edit-ed/13-UI-SPEC.md
  </read_first>
  <action>
Create `useMarketplaceItemsView.ts` exporting `type MarketplaceItemsView = "table" | "grid"` and `useMarketplaceItemsView()`.
Use constant `STORAGE_KEY = "relique.admin.marketplace.itemsView"`.
Return `{ view, setView, mounted }`.
On mount, read localStorage in try/catch. Accept only `"table"` or `"grid"`.
If no valid stored value, use `window.matchMedia("(max-width: 767px)").matches ? "grid" : "table"`.
When `setView(next)` is called, update React state and write to localStorage in try/catch.
Before mount, initial state must be `"table"` to avoid SSR mismatch.
  </action>
  <acceptance_criteria>
    - `src/features/marketplace/hooks/useMarketplaceItemsView.ts` contains `relique.admin.marketplace.itemsView`
    - `src/features/marketplace/hooks/useMarketplaceItemsView.ts` contains `matchMedia("(max-width: 767px)")`
    - `src/features/marketplace/hooks/useMarketplaceItemsView.ts` contains `try` and `catch`
    - `npx eslint --max-warnings 0 src/features/marketplace/hooks/useMarketplaceItemsView.ts` exits 0
  </acceptance_criteria>
</task>

<task id="13-01-02" type="auto">
  <name>Add view toggle and count summary components</name>
  <files>
    src/components/admin/marketplace/items/MarketplaceItemsViewToggle.tsx
    src/components/admin/marketplace/items/MarketplaceItemsCountSummary.tsx
  </files>
  <read_first>
    - src/components/admin/marketplace/items/MarketplaceItemsToolbar.tsx
    - src/components/admin/marketplace/items/MarketplaceItemsPagination.tsx
    - src/features/marketplace/services/marketplaceService.ts
    - .planning/phases/13-marketplace-items-table-grid-toggle-full-page-create-edit-ed/13-UI-SPEC.md
  </read_first>
  <action>
Create `MarketplaceItemsViewToggle.tsx` as a client component with props `{ view: "table" | "grid"; onViewChange: (view) => void; disabled?: boolean }`.
Render two segmented buttons labeled `Table` and `Grid` with lucide `Table2` and `Grid3X3` icons.
Use `aria-pressed`, visible focus rings, `bg-primary text-white` for active, and `bg-white/5 border border-border text-gray-300` for inactive.

Create `MarketplaceItemsCountSummary.tsx` with props `{ visible: number; filtered: number; total: number; hasFilters: boolean }`.
Render exact text:
- filters active: `Showing {visible} of {filtered} filtered items`
- no filters: `Showing {visible} of {total} items`
Use `text-sm text-gray-400`.
  </action>
  <acceptance_criteria>
    - `MarketplaceItemsViewToggle.tsx` contains `aria-pressed`
    - `MarketplaceItemsViewToggle.tsx` contains `Table2`
    - `MarketplaceItemsViewToggle.tsx` contains `Grid3X3`
    - `MarketplaceItemsCountSummary.tsx` contains `Showing`
    - `npx eslint --max-warnings 0 src/components/admin/marketplace/items/MarketplaceItemsViewToggle.tsx src/components/admin/marketplace/items/MarketplaceItemsCountSummary.tsx` exits 0
  </acceptance_criteria>
</task>

<task id="13-01-03" type="auto">
  <name>Wire toggle and counts into toolbar/page</name>
  <files>
    src/components/admin/marketplace/items/MarketplaceItemsToolbar.tsx
    src/components/admin/marketplace/pages/ItemsPage.tsx
  </files>
  <read_first>
    - src/components/admin/marketplace/items/MarketplaceItemsToolbar.tsx
    - src/components/admin/marketplace/pages/ItemsPage.tsx
    - src/features/marketplace/hooks/useMarketplaceItemsView.ts
    - src/components/admin/marketplace/items/MarketplaceItemsViewToggle.tsx
    - src/components/admin/marketplace/items/MarketplaceItemsCountSummary.tsx
  </read_first>
  <action>
Extend `MarketplaceItemsToolbarProps` with `view`, `onViewChange`, and optional `viewToggleDisabled`.
Render `MarketplaceItemsViewToggle` in the toolbar controls after status tabs on desktop and in the wrapping controls on mobile.
In `ItemsPage`, call `useMarketplaceItemsView()`.
Compute:
- `visible = items.length`
- `filtered = data?.total ?? 0`
- `total = data?.counts?.all ?? filtered`
Render `MarketplaceItemsCountSummary` below toolbar and before filter chips.
Do not move URL state, query state, or `selectedIds` out of `ItemsPage`.
  </action>
  <acceptance_criteria>
    - `ItemsPage.tsx` contains `useMarketplaceItemsView`
    - `ItemsPage.tsx` contains `MarketplaceItemsCountSummary`
    - `MarketplaceItemsToolbar.tsx` contains `MarketplaceItemsViewToggle`
    - `ItemsPage.tsx` still contains `selectedIds`
    - `npx eslint --max-warnings 0 src/components/admin/marketplace/items/MarketplaceItemsToolbar.tsx src/components/admin/marketplace/pages/ItemsPage.tsx` exits 0
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run targeted lint for all files in this plan. Confirm the table view still renders when `view === "table"` and no Grid component exists yet.
</verification>

<success_criteria>
- View hook is SSR-safe and localStorage-safe.
- Toggle is keyboard-focusable and visually indicates selected view.
- Count summary renders from existing list response data.
- Phase 12 filters, sort, pagination, and selected IDs remain owned by `ItemsPage`.
</success_criteria>


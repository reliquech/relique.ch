---
phase: 13-marketplace-items-table-grid-toggle-full-page-create-edit-ed
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/components/admin/marketplace/items/MarketplaceItemsGrid.tsx
  - src/components/admin/marketplace/items/MarketplaceItemsGridCard.tsx
  - src/components/admin/marketplace/pages/ItemsPage.tsx
autonomous: true
requirements:
  - ITEMS-13-02
must_haves:
  truths:
    - "Grid view renders marketplace item cards"
    - "Grid cards share selectedIds with the table"
    - "Grid card body opens edit while checkbox/menu stop propagation"
    - "Grid layout is responsive from 1 to 4+ columns"
---

<objective>
Add responsive Grid view cards for Marketplace Items, reusing Phase 12 selection, bulk bar, row menu, and row action contracts.
</objective>

<threat_model>
T13-02: Grid cards can trigger unintended destructive actions if event propagation is wrong. Checkbox and menu interactions must call `stopPropagation`.
T13-03: Rendering image URLs from data must not introduce unbounded layout shift. Use stable aspect-ratio containers and existing Next/Image or safe placeholder behavior.
</threat_model>

<tasks>
<task id="13-02-01" type="auto">
  <name>Create grid card component</name>
  <files>src/components/admin/marketplace/items/MarketplaceItemsGridCard.tsx</files>
  <read_first>
    - src/components/admin/marketplace/items/MarketplaceItemsTable.tsx
    - src/components/admin/marketplace/items/MarketplaceItemRowMenu.tsx
    - src/lib/types/admin.ts
    - src/lib/utils/admin.tsx
    - .planning/phases/13-marketplace-items-table-grid-toggle-full-page-create-edit-ed/13-UI-SPEC.md
  </read_first>
  <action>
Create `MarketplaceItemsGridCard.tsx` as a client component.
Props:
`item: MarketplaceItem`, `selected: boolean`, `onToggle: (id: string, checked: boolean) => void`, `onOpen: (id: string) => void`, `onRowAction` matching `MarketplaceItemsTable`.
Render:
- stable image area with `aspect-[4/3]`, `cover_image_url` when HTTP(S), otherwise placeholder `ImageIcon`
- checkbox with label `Select {item.title}` and `onClick={(e) => e.stopPropagation()}`
- title two-line clamp with `title={item.title}`
- athlete, price, status pill from `getStatusPill`, featured star, updated date
- `MarketplaceItemRowMenu` in a menu cell that stops propagation
Card `onClick` calls `onOpen(item.id)`. Card `onKeyDown` opens on Enter.
Selected class: `bg-primary/10 ring-1 ring-primary/30`.
  </action>
  <acceptance_criteria>
    - `MarketplaceItemsGridCard.tsx` contains `aspect-[4/3]`
    - `MarketplaceItemsGridCard.tsx` contains `stopPropagation`
    - `MarketplaceItemsGridCard.tsx` contains `MarketplaceItemRowMenu`
    - `MarketplaceItemsGridCard.tsx` contains `getStatusPill`
    - `npx eslint --max-warnings 0 src/components/admin/marketplace/items/MarketplaceItemsGridCard.tsx` exits 0
  </acceptance_criteria>
</task>

<task id="13-02-02" type="auto">
  <name>Create grid container</name>
  <files>src/components/admin/marketplace/items/MarketplaceItemsGrid.tsx</files>
  <read_first>
    - src/components/admin/marketplace/items/MarketplaceItemsGridCard.tsx
    - src/components/admin/marketplace/items/MarketplaceItemsTable.tsx
    - src/lib/types/admin.ts
  </read_first>
  <action>
Create `MarketplaceItemsGrid.tsx` as a client component.
Accept the same core props as table: `items`, `selectedIds`, `onToggleOne`, `onRowClick`, `onRowAction`.
Render a grid with classes equivalent to `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 2xl:grid-cols-4 gap-4`.
For each item, render `MarketplaceItemsGridCard`.
Do not own selection state locally.
  </action>
  <acceptance_criteria>
    - `MarketplaceItemsGrid.tsx` contains `selectedIds`
    - `MarketplaceItemsGrid.tsx` contains `MarketplaceItemsGridCard`
    - `MarketplaceItemsGrid.tsx` contains `sm:grid-cols-2`
    - `MarketplaceItemsGrid.tsx` contains `2xl:grid-cols-4`
    - `npx eslint --max-warnings 0 src/components/admin/marketplace/items/MarketplaceItemsGrid.tsx` exits 0
  </acceptance_criteria>
</task>

<task id="13-02-03" type="auto">
  <name>Render table or grid from shared state</name>
  <files>src/components/admin/marketplace/pages/ItemsPage.tsx</files>
  <read_first>
    - src/components/admin/marketplace/pages/ItemsPage.tsx
    - src/components/admin/marketplace/items/MarketplaceItemsTable.tsx
    - src/components/admin/marketplace/items/MarketplaceItemsGrid.tsx
    - src/features/marketplace/hooks/useMarketplaceItemsView.ts
  </read_first>
  <action>
Import `MarketplaceItemsGrid`.
In the loaded/non-empty branch, render:
- `MarketplaceItemsTable` when `view === "table"`
- `MarketplaceItemsGrid` when `view === "grid"`
Pass the same `items`, `selectedIds`, `onToggleOne`, `onRowClick`, and `rowActions` to Grid.
Keep pagination below both views.
Keep the existing table component unchanged except for integration needs.
  </action>
  <acceptance_criteria>
    - `ItemsPage.tsx` contains `MarketplaceItemsGrid`
    - `ItemsPage.tsx` contains `view === "table"`
    - `ItemsPage.tsx` contains `view === "grid"`
    - `ItemsPage.tsx` passes `selectedIds={selectedIds}` to both table and grid paths
    - `npx eslint --max-warnings 0 src/components/admin/marketplace/pages/ItemsPage.tsx src/components/admin/marketplace/items/MarketplaceItemsGrid.tsx src/components/admin/marketplace/items/MarketplaceItemsGridCard.tsx` exits 0
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run targeted lint. Manual UAT later must select an item in Table, switch to Grid, and confirm the selected card and bulk bar remain.
</verification>

<success_criteria>
- Grid renders all required card metadata.
- Selection and row action contracts are shared with Table.
- View switching does not clear filters, pagination, or selected IDs.
</success_criteria>


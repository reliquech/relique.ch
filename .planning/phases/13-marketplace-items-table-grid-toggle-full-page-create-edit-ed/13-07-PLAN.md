---
phase: 13-marketplace-items-table-grid-toggle-full-page-create-edit-ed
plan: 07
type: execute
wave: 4
depends_on:
  - 13-05
  - 13-06
files_modified:
  - src/components/admin/marketplace/editor/MarketplacePublishConfirmDialog.tsx
  - src/components/admin/marketplace/editor/MarketplaceEditorPage.tsx
  - src/features/marketplace/services/marketplaceEditorService.ts
  - src/components/admin/marketplace/pages/ItemsPage.tsx
autonomous: true
requirements:
  - ITEMS-13-03
  - ITEMS-13-05
must_haves:
  truths:
    - "Publish requires explicit confirmation"
    - "Editor action bar prevents publish while invalid, offline, uploading, or saving"
    - "Manual save, autosave, publish, cancel, and back flows have clear user feedback"
    - "List reflects editor-created or edited items after navigation"
---

<objective>
Finalize editor action flows: publish confirmation, action bar gating, navigation behavior, and list refresh consistency.
</objective>

<threat_model>
T13-15: Publishing incomplete or stale data damages marketplace trust. Confirm publish explicitly and block while invalid, offline, uploading, or saving.
T13-16: Draft creation from autosave can navigate unexpectedly. Keep autosave non-navigating; navigation happens only on user actions.
T13-17: Returning to list after create/edit can show stale data. Trigger existing query refresh patterns or rely on URL navigation plus service refetch.
</threat_model>

<tasks>
<task id="13-07-01" type="auto">
  <name>Create publish confirmation dialog</name>
  <files>src/components/admin/marketplace/editor/MarketplacePublishConfirmDialog.tsx</files>
  <read_first>
    - src/components/admin/marketplace/items/MarketplaceTypedDeleteDialog.tsx
    - src/components/ui/dialog.tsx
    - src/components/ui/button.tsx
    - .planning/phases/13-marketplace-items-table-grid-toggle-full-page-create-edit-ed/13-UI-SPEC.md
  </read_first>
  <action>
Create `MarketplacePublishConfirmDialog.tsx`.
Props:
`open`, `onOpenChange`, `title`, `status`, `onConfirm`, `isPublishing?: boolean`.
Explain that publishing makes the item visible wherever published marketplace items are surfaced.
Confirm button label: `Publish item`.
Use existing dialog primitives without modifying `src/components/ui/**`.
  </action>
  <acceptance_criteria>
    - `MarketplacePublishConfirmDialog.tsx` contains `Publish item`
    - `MarketplacePublishConfirmDialog.tsx` contains `onConfirm`
    - `MarketplacePublishConfirmDialog.tsx` imports from `@/components/ui/dialog`
    - `npx eslint --max-warnings 0 src/components/admin/marketplace/editor/MarketplacePublishConfirmDialog.tsx` exits 0
  </acceptance_criteria>
</task>

<task id="13-07-02" type="auto">
  <name>Wire publish confirmation and action states</name>
  <files>
    src/components/admin/marketplace/editor/MarketplaceEditorPage.tsx
    src/features/marketplace/services/marketplaceEditorService.ts
  </files>
  <read_first>
    - src/components/admin/marketplace/editor/MarketplaceEditorPage.tsx
    - src/components/admin/marketplace/editor/MarketplaceEditorStatusRail.tsx
    - src/components/admin/marketplace/editor/MarketplacePublishConfirmDialog.tsx
    - src/features/marketplace/services/marketplaceEditorService.ts
  </read_first>
  <action>
Wire `MarketplacePublishConfirmDialog` into `MarketplaceEditorPage`.
Publish flow:
1. Validate form with `trigger()`.
2. Open confirmation only if valid and action gating passes.
3. On confirm, save payload with status `published`.
4. Show success toast and navigate back to `/admin/items` or the canonical items route already used by the admin.
Manual Save Draft must save with status `draft`, show toast, and stay on editor.
Cancel/back must use unsaved guard confirmation when dirty.
  </action>
  <acceptance_criteria>
    - `MarketplaceEditorPage.tsx` contains `MarketplacePublishConfirmDialog`
    - `MarketplaceEditorPage.tsx` contains `trigger()`
    - `MarketplaceEditorPage.tsx` contains `status`
    - `MarketplaceEditorPage.tsx` contains a success toast
    - `npx eslint --max-warnings 0 src/components/admin/marketplace/editor/MarketplaceEditorPage.tsx src/features/marketplace/services/marketplaceEditorService.ts` exits 0
  </acceptance_criteria>
</task>

<task id="13-07-03" type="auto">
  <name>Validate list return path and refresh behavior</name>
  <files>src/components/admin/marketplace/pages/ItemsPage.tsx</files>
  <read_first>
    - src/components/admin/marketplace/pages/ItemsPage.tsx
    - src/features/marketplace/hooks/useMarketplaceItemsQuery.ts
    - src/components/admin/marketplace/pages/NewMarketplacePage.tsx
    - src/components/admin/marketplace/pages/EditMarketplacePage.tsx
  </read_first>
  <action>
Ensure editor-created and edited items are visible after returning to the Items list.
If `ItemsPage` already refetches on mount/navigation, document that by leaving code unchanged.
If stale data persists, add the smallest refresh mechanism consistent with `useMarketplaceItemsQuery`, such as calling existing `refetch` on navigation events or invalidating local list state.
Do not introduce React Query/SWR.
  </action>
  <acceptance_criteria>
    - create/edit publish flow returns to the correct items route
    - list query still uses `useMarketplaceItemsQuery`
    - no new query library is introduced
    - `npx eslint --max-warnings 0 src/components/admin/marketplace/pages/ItemsPage.tsx` exits 0
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run targeted checks:
- `npx eslint --max-warnings 0 src/components/admin/marketplace/editor/MarketplacePublishConfirmDialog.tsx src/components/admin/marketplace/editor/MarketplaceEditorPage.tsx src/features/marketplace/services/marketplaceEditorService.ts src/components/admin/marketplace/pages/ItemsPage.tsx`
- `npm run check-types`
Manual UAT:
- Save draft from create.
- Publish only after confirm.
- Cancel dirty editor and verify guard appears.
- Return to list and verify the changed item appears.
</verification>

<success_criteria>
- Publish cannot bypass confirmation or action gating.
- Draft save stays in editor.
- Publish navigates to list and shows updated data.
- Unsaved navigation protection works for browser and internal cancel/back paths.
</success_criteria>

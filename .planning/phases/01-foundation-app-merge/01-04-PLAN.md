---
phase: 01-foundation-app-merge
plan: 04
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - apps/web/src/admin/crm/index.ts
  - apps/web/src/admin/marketplace/index.ts
  - apps/web/src/admin/dashboard/index.ts
  - apps/web/src/components/admin/AdminPortalLayout.tsx
  - apps/web/src/components/admin/PortalSidebar.tsx
  - apps/web/src/app/admin/(portal)/layout.tsx
  - apps/web/src/app/admin/(portal)/page.tsx
  - apps/web/src/app/admin/(portal)/deals/page.tsx
  - apps/web/src/app/admin/(portal)/leads/page.tsx
  - apps/web/src/app/layout.tsx
autonomous: false
requirements:
  - FND-01
  - FND-02
must_haves:
  truths:
    - "Operator opens /admin and sees CRM sidebar — no public Header/Footer"
    - "/admin/login has no sidebar (outside portal group)"
    - "21 admin pages accessible via thin route re-exports"
    - "Admin modules live at src/admin/ not src/features/ (D-01)"
    - "CRM pages fetch data from /api/* on unified app"
  artifacts:
    - path: "apps/web/src/admin/crm/index.ts"
      provides: "CRM module barrel export"
    - path: "apps/web/src/components/admin/AdminPortalLayout.tsx"
      provides: "Admin shell with sidebar"
    - path: "apps/web/src/app/admin/(portal)/layout.tsx"
      provides: "Portal layout wrapper"
    - path: "apps/web/src/app/admin/(portal)/deals/page.tsx"
      provides: "Thin re-export to admin page"
  key_links:
    - from: "apps/web/src/app/admin/(portal)/deals/page.tsx"
      to: "@/admin/crm/pages/DealsPage"
      via: "re-export"
      pattern: '@/admin/crm'
    - from: "apps/web/src/components/admin/AdminPortalLayout.tsx"
      to: "@/components/admin/PortalSidebar"
      via: "render"
      pattern: "PortalSidebar"
    - from: "apps/web/src/admin/crm/services/dealsService.ts"
      to: "/api/deals"
      via: "fetch"
      pattern: "fetch.*['\x22]/api/deals"
---

<objective>
Migrate admin UI modules và shell sang unified app — hoàn tất FND-01 và FND-02.

Purpose: API layer ready (Wave 3); now wire 8 feature modules + 21 pages + admin shell per D-01–D-04.
Output: `src/admin/*` modules, `components/admin/*` shell, `app/admin/(portal)/*` thin routes, CommandPalette wired.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-app-merge/01-CONTEXT.md
@apps/admin/src/features/crm/index.ts
@apps/admin/src/app/admin/layout.tsx
@apps/admin/src/components/shell/PortalSidebar.tsx

<interfaces>
8 admin modules to copy (apps/admin/src/features/ → apps/web/src/admin/):
- crm, marketplace, submissions, dashboard, tasks, notifications, automations, users

21 admin pages (thin re-export pattern per D-04):
- /admin (dashboard), deals, leads, customers, tasks, items, marketplace/new, featured, submissions, submissions/consign, submissions/authenticate, users, profile, settings, messages, logs, custom-fields, pipeline-stages, automations, verify, marketplace/agent-create

Import migration map:
- `@/features/crm/...` → `@/admin/crm/...`
- `@/features/marketplace/...` → `@/admin/marketplace/...`
- `@/components/shell/PortalSidebar` → `@/components/admin/PortalSidebar`

Route structure (D-03, Pitfall 2):
- `app/admin/login/page.tsx` — already Wave 2, NO sidebar
- `app/admin/(portal)/layout.tsx` — sidebar shell only
- `app/admin/(portal)/**/page.tsx` — thin re-exports

shadcn-guard: copy admin ui components to web `components/ui/` as-is if needed for build; do NOT edit ui primitives.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy admin modules to src/admin with import rewrites</name>
  <read_first>
    - apps/admin/src/features/crm/index.ts
    - apps/admin/src/features/marketplace/index.ts
    - apps/admin/src/features/dashboard/index.ts
    - apps/admin/src/features/submissions/index.ts
    - apps/admin/src/features/tasks/index.ts
    - apps/admin/src/features/notifications/index.ts
    - apps/admin/src/features/automations/index.ts
    - apps/admin/src/features/users/index.ts
  </read_first>
  <files>
    apps/web/src/admin/crm/index.ts
    apps/web/src/admin/marketplace/index.ts
    apps/web/src/admin/dashboard/index.ts
    apps/web/src/admin/submissions/index.ts
    apps/web/src/admin/tasks/index.ts
    apps/web/src/admin/notifications/index.ts
    apps/web/src/admin/automations/index.ts
    apps/web/src/admin/users/index.ts
  </files>
  <action>
    Per D-01, D-02:

    1. Copy entire `apps/admin/src/features/*` → `apps/web/src/admin/*` preserving structure:
       - Each domain: pages/, components/, services/, hooks/, types.ts, schemas.ts, index.ts

    2. Bulk rewrite imports across copied files:
       - `@/features/` → `@/admin/`
       - `@/components/shell/PortalSidebar` → `@/components/admin/PortalSidebar` (after Task 2 creates it)
       - Keep `@/lib/supabase/`, `@/lib/actions/`, `@relique/shared`, `@relique/ui` unchanged

    3. Copy admin `components/shared/` items referenced by admin modules into `apps/web/src/components/shared/` (merge, don't overwrite web shared if conflict — rename admin-specific)

    4. Copy missing `components/ui/` from admin to web if build requires (as-is, no edits per shadcn-guard)

    5. Split files >300 lines only if build fails — prefer deferring to Phase 5 UX

    6. Copy admin public assets to `apps/web/public/` (background images referenced in layout)

    Do NOT use `src/features/` path anywhere (D-01).
    Do NOT import from `apps/admin/` at runtime.
  </action>
  <acceptance_criteria>
    - `test -d apps/web/src/admin/crm` exits 0
    - `test -d apps/web/src/admin/marketplace` exits 0
    - `test -f apps/web/src/admin/crm/index.ts` exits 0
    - `! grep -rq "@/features/" apps/web/src/admin` exits 0
    - `grep -rq "@/admin/" apps/web/src/admin` exits 0
    - `find apps/web/src/admin -name index.ts | wc -l | awk '{print ($1 >= 8)}'` exits 0
    - `pnpm --filter web check-types` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web check-types</automated>
  </verify>
  <done>8 admin modules at src/admin/ with @/admin/ imports; no @/features/ references</done>
</task>

<task type="auto">
  <name>Task 2: Create admin shell and portal route re-exports</name>
  <read_first>
    - apps/admin/src/app/admin/layout.tsx
    - apps/admin/src/components/shell/PortalSidebar.tsx
    - apps/admin/src/app/admin/deals/page.tsx
    - apps/admin/src/lib/actions/actionRegistry.ts
    - .planning/phases/01-foundation-app-merge/01-CONTEXT.md (D-03, D-04)
  </read_first>
  <files>
    apps/web/src/components/admin/AdminPortalLayout.tsx
    apps/web/src/components/admin/PortalSidebar.tsx
    apps/web/src/app/admin/(portal)/layout.tsx
    apps/web/src/app/admin/(portal)/page.tsx
    apps/web/src/app/admin/(portal)/deals/page.tsx
    apps/web/src/app/admin/(portal)/leads/page.tsx
    apps/web/src/app/admin/(portal)/customers/page.tsx
    apps/web/src/app/admin/(portal)/tasks/page.tsx
    apps/web/src/app/admin/(portal)/items/page.tsx
    apps/web/src/app/admin/(portal)/marketplace/new/page.tsx
    apps/web/src/app/admin/(portal)/featured/page.tsx
    apps/web/src/app/admin/(portal)/submissions/page.tsx
    apps/web/src/app/admin/(portal)/users/page.tsx
    apps/web/src/app/admin/(portal)/profile/page.tsx
    apps/web/src/app/admin/(portal)/settings/page.tsx
    apps/web/src/app/admin/(portal)/messages/page.tsx
    apps/web/src/app/admin/(portal)/logs/page.tsx
    apps/web/src/app/admin/(portal)/custom-fields/page.tsx
    apps/web/src/app/admin/(portal)/pipeline-stages/page.tsx
    apps/web/src/app/admin/(portal)/automations/page.tsx
    apps/web/src/app/admin/(portal)/verify/page.tsx
    apps/web/src/app/layout.tsx
  </files>
  <action>
    Per D-03, D-04:

    1. Copy `PortalSidebar` and admin chrome from `apps/admin/src/components/shell/` → `apps/web/src/components/admin/`
    2. Create `AdminPortalLayout.tsx` wrapper — adapt from `apps/admin/src/app/admin/layout.tsx`:
       - Import PortalSidebar from `@/components/admin/PortalSidebar`
       - Import admin modules from `@/admin/notifications`, `@/admin/automations`, `@/admin/users`
       - Update logout/login links to `/admin/login` (not `/login`)
       - Keep under 300 lines — extract subcomponents if needed

    3. Create portal routes:
       - `app/admin/(portal)/layout.tsx` → `export { default } from "@/components/admin/AdminPortalLayout"`
       - Create 21 thin `page.tsx` files re-exporting from `@/admin/{domain}/pages/*`
       - Map routes per admin inventory (deals→crm, items→marketplace, etc.)

    4. Wire CommandPalette in root layout:
       - Add CommandPalette to `apps/web/src/app/layout.tsx` (harmless on public per RESEARCH discretion)
       - Ensure `initializeActions()` called — import from `@/lib/actions/actionRegistry`

    5. Verify `/admin/login` remains OUTSIDE `(portal)/` — no sidebar on login

    shadcn-guard: PortalSidebar wraps ui primitives, do not edit `components/ui/**`.
  </action>
  <acceptance_criteria>
    - `test -f apps/web/src/components/admin/AdminPortalLayout.tsx` exits 0
    - `test -f apps/web/src/components/admin/PortalSidebar.tsx` exits 0
    - `test -f apps/web/src/app/admin/(portal)/layout.tsx` exits 0
    - `test -f apps/web/src/app/admin/(portal)/deals/page.tsx` exits 0
    - `grep -q '@/admin/crm' apps/web/src/app/admin/(portal)/deals/page.tsx` exits 0
    - `find apps/web/src/app/admin/(portal) -name page.tsx | wc -l | awk '{print ($1 >= 20)}'` exits 0
    - `! test -f apps/web/src/app/admin/(portal)/login/page.tsx` exits 0 (login outside portal)
    - `grep -q "CommandPalette" apps/web/src/app/layout.tsx` exits 0
    - `pnpm --filter web build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web build</automated>
  </verify>
  <done>Admin portal shell wired; 21 pages re-export; CommandPalette in root layout; build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify admin UI shell and data loading</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 success criteria)
    - .planning/phases/01-foundation-app-merge/01-VALIDATION.md
  </read_first>
  <files>N/A — verification only</files>
  <action>
    Run automated pre-checks, then pause for human UAT.
    1. Execute `pnpm --filter web build` — must pass before human verification
    2. Start dev server: `pnpm --filter web dev`
    3. Present what-built summary and how-to-verify steps to user
    4. Wait for "approved" or issue report before marking plan complete
  </action>
  <what-built>Complete admin UI: sidebar shell at /admin, 21 CRM pages, no public chrome on admin routes, API data fetching</what-built>
  <how-to-verify>
    1. Run `pnpm dev:web` (or `pnpm dev` after Wave 5)
    2. Visit http://localhost:1300/admin logged out → redirects to `/admin/login?redirect=/admin`
    3. Login at `/admin/login` → lands on `/admin` dashboard (NOT redirect param destination)
    4. Confirm: sidebar visible, NO public site Header/Footer
    5. Navigate to /admin/leads, /admin/deals, /admin/items — pages load data (no 401 toast errors)
    6. Visit http://localhost:1300/marketplace — public Header/Footer still present
    7. Browser MCP screenshot of /admin dashboard for visual record
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms sidebar visible at /admin without public Header/Footer
    - Human confirms CRM pages load data from /api/*
    - Human confirms public /marketplace unchanged
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web build</automated>
  </verify>
  <done>Human approves admin UI shell, data loading, and public site isolation</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → /api/* | Client fetches with session cookies |
| Public (site) layout | Must not render on /admin routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | AdminPortalLayout | mitigate | useProfile for display only; API enforces auth |
| T-1-02 | Tampering | Client services | accept | Server-side requireUser on all mutations |
</threat_model>

<verification>
- `pnpm --filter web build` passes
- 8 modules at src/admin/, 21 portal pages, admin shell at components/admin/
- Human UAT: admin sidebar, no public chrome, CRM data loads
</verification>

<success_criteria>
- FND-01: Admin CRM operational on unified app
- FND-02: Admin layout isolated from public shell
- All 21 admin pages accessible with thin re-exports
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-app-merge/01-04-SUMMARY.md`
</output>

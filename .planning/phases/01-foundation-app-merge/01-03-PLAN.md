---
phase: 01-foundation-app-merge
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - apps/web/src/lib/observability/clientErrorLog.ts
  - apps/web/src/lib/observability/serverErrorLog.ts
  - apps/web/src/lib/actions/actionRegistry.ts
  - apps/web/src/app/api/deals/route.ts
  - apps/web/src/app/api/leads/route.ts
  - apps/web/src/app/api/marketplace/route.ts
  - apps/web/src/app/api/marketplace/[id]/route.ts
  - apps/web/src/app/api/marketplace/upload/route.ts
  - apps/web/src/app/api/marketplace/agent-create/route.ts
autonomous: true
requirements:
  - FND-04
must_haves:
  truths:
    - "~56 admin API routes copied to apps/web/src/app/api/ (excluding marketplace/route.ts merge)"
    - "Admin API routes return 401 without valid session via requireUser()"
    - "GET /api/marketplace works anonymously (published-only) AND authenticated (admin list)"
    - "POST/PATCH/DELETE /api/marketplace require auth"
  artifacts:
    - path: "apps/web/src/app/api/deals/route.ts"
      provides: "CRM deals API"
      exports: ["GET", "POST"]
    - path: "apps/web/src/app/api/marketplace/route.ts"
      provides: "Merged dual-GET marketplace handler"
      exports: ["GET", "POST", "PATCH"]
    - path: "apps/web/src/lib/supabase/requireUser.ts"
      provides: "Used by all admin API routes"
  key_links:
    - from: "apps/web/src/app/api/deals/route.ts"
      to: "requireUser"
      via: "import and call at handler entry"
      pattern: "requireUser"
    - from: "apps/web/src/app/api/marketplace/route.ts"
      to: "public published filter OR admin query"
      via: "GET branches on session"
      pattern: "requireUser|published"
---

<objective>
Migrate ~57 admin API routes sang unified app và merge conflict `/api/marketplace` dual-GET.

Purpose: Admin CRM pages depend on `/api/*` handlers (FND-04). Web đã có public marketplace API — cần merge không break public browse.
Output: Full admin API namespace in `apps/web/src/app/api/`; supporting libs ported; marketplace dual-path GET.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-app-merge/01-RESEARCH.md
@apps/admin/src/app/api/marketplace/route.ts
@apps/web/src/app/api/marketplace/route.ts
@apps/web/src/app/api/marketplace/utils.ts

<interfaces>
Marketplace merge strategy (Pattern 5 from RESEARCH):
- GET: try requireUser() — if auth OK → admin list (all lifecycle); if not → public published-only (existing web logic + mapRowToListing)
- POST/PATCH/DELETE: always requireUser() + requireRole where applicable

Admin API inventory (57 route.ts files):
activity, alert-rules, attachments, audit-logs, auth/register, consigned, crm-*, custom-fields, customers, dashboard, deals, email, error-log, health, leads, marketplace/*, messages, notification-preferences, notifications, pipeline-stages, tasks, users

Existing web API (keep, do not overwrite blindly):
- apps/web/src/app/api/marketplace/route.ts (public GET)
- apps/web/src/app/api/marketplace/[slug]/route.ts (public detail)
- apps/web/src/app/api/article-meta/route.ts

Copy supporting libs from admin:
- apps/admin/src/lib/observability/
- apps/admin/src/lib/actions/
- apps/admin/src/lib/utils/ (admin-specific helpers)
- apps/admin/src/lib/types/ (if referenced by API routes)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port supporting libs and copy admin API routes</name>
  <read_first>
    - apps/admin/src/lib/observability/clientErrorLog.ts
    - apps/admin/src/lib/observability/serverErrorLog.ts
    - apps/admin/src/lib/actions/actionRegistry.ts
    - apps/admin/src/app/api/deals/route.ts
    - apps/admin/src/lib/supabase/requireUser.ts
  </read_first>
  <files>
    apps/web/src/lib/observability/clientErrorLog.ts
    apps/web/src/lib/observability/serverErrorLog.ts
    apps/web/src/lib/actions/actionRegistry.ts
    apps/web/src/lib/utils/admin.ts
    apps/web/src/lib/types/admin.ts
    apps/web/src/app/api/activity/route.ts
    apps/web/src/app/api/activity/note/route.ts
    apps/web/src/app/api/alert-rules/route.ts
    apps/web/src/app/api/attachments/route.ts
    apps/web/src/app/api/audit-logs/route.ts
    apps/web/src/app/api/auth/register/route.ts
    apps/web/src/app/api/consigned/route.ts
    apps/web/src/app/api/crm-filters/route.ts
    apps/web/src/app/api/crm-views/route.ts
    apps/web/src/app/api/custom-fields/route.ts
    apps/web/src/app/api/customers/route.ts
    apps/web/src/app/api/dashboard/route.ts
    apps/web/src/app/api/deals/route.ts
    apps/web/src/app/api/email/send/route.ts
    apps/web/src/app/api/error-log/route.ts
    apps/web/src/app/api/health/route.ts
    apps/web/src/app/api/leads/route.ts
    apps/web/src/app/api/messages/route.ts
    apps/web/src/app/api/notifications/route.ts
    apps/web/src/app/api/pipeline-stages/route.ts
    apps/web/src/app/api/tasks/route.ts
    apps/web/src/app/api/users/route.ts
  </files>
  <action>
    1. Copy `apps/admin/src/lib/observability/` → `apps/web/src/lib/observability/`
    2. Copy `apps/admin/src/lib/actions/` → `apps/web/src/lib/actions/` (actionRegistry + domain action files)
    3. Copy admin-specific utils/types referenced by API routes (e.g. `lib/utils/admin.ts`, CRM helpers) — grep admin API imports to find all needed files
    4. Copy ALL `apps/admin/src/app/api/**` route files to `apps/web/src/app/api/**` EXCEPT:
       - `marketplace/route.ts` (handled in Task 2)
       - Do NOT overwrite `apps/web/src/app/api/article-meta/route.ts`
       - Do NOT overwrite `apps/web/src/app/api/marketplace/[slug]/route.ts` (public detail by slug)
    5. For marketplace sub-routes FROM admin, copy/merge:
       - `[id]/route.ts`, `[id]/status/route.ts`, `upload/`, `agent-create/` → web (admin versions)
       - Keep web `[slug]/route.ts` for public detail
    6. Every copied route MUST call `requireUser()` at handler entry (verify grep on sample routes)
    7. Fix import paths — all should use `@/lib/supabase/*` (same alias root)

    Known gap (document, do not fix): `/api/auth/register` remains public — SEC-01 deferred Phase 3.

    Do NOT import from `apps/admin/` at runtime — copy files only.
  </action>
  <acceptance_criteria>
    - `find apps/web/src/app/api -name route.ts | wc -l | awk '{print ($1 >= 55)}'` exits 0 (at least 55 API route files)
    - `test -f apps/web/src/app/api/deals/route.ts` exits 0
    - `test -f apps/web/src/app/api/leads/route.ts` exits 0
    - `grep -rq "requireUser" apps/web/src/app/api/deals/route.ts` exits 0
    - `test -f apps/web/src/lib/observability/serverErrorLog.ts` exits 0
    - `test -f apps/web/src/lib/actions/actionRegistry.ts` exits 0
    - `! grep -rq "from.*apps/admin" apps/web/src/app/api` exits 0 (no cross-app imports)
    - `pnpm --filter web check-types` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web check-types</automated>
  </verify>
  <done>Admin API routes and supporting libs exist in web; requireUser pattern present</done>
</task>

<task type="auto">
  <name>Task 2: Merge marketplace dual-GET handler</name>
  <read_first>
    - apps/web/src/app/api/marketplace/route.ts
    - apps/admin/src/app/api/marketplace/route.ts
    - apps/web/src/app/api/marketplace/utils.ts
    - .planning/phases/01-foundation-app-merge/01-RESEARCH.md (Pattern 5)
  </read_first>
  <files>
    apps/web/src/app/api/marketplace/route.ts
  </files>
  <action>
    Merge into single `apps/web/src/app/api/marketplace/route.ts`:

    **GET handler dual-path:**
    1. Attempt session check via `createClient()` from `@/lib/supabase/server` + `auth.getUser()`
    2. If authenticated user exists → run admin GET logic from `apps/admin/src/app/api/marketplace/route.ts`:
       - requireUser() guard (defense in depth)
       - Support status/category/featured query params
       - Return raw admin row shape expected by admin services
    3. If NOT authenticated → run existing web public GET logic:
       - Filter `state_lifecycle = published` AND visibility public
       - Use `mapRowToListing` from `./utils`
       - Preserve existing response shape for public marketplace

    **Write handlers (POST, PATCH, DELETE if present):**
    - Always `requireUser()` + `requireRole({ allow: ['admin', 'editor'] })` where admin route had role check
    - Copy mutation logic from admin route.ts

    **Regression prevention:**
    - Anonymous GET must NOT call requireUser() in a way that returns 401
    - Authenticated GET must NOT apply published-only filter

    Document manual curl test cases in SUMMARY:
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:1300/api/marketplace` → 200
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:1300/api/deals` → 401
  </action>
  <acceptance_criteria>
    - `grep -q "GET" apps/web/src/app/api/marketplace/route.ts` exits 0
    - `grep -q "mapRowToListing\|published" apps/web/src/app/api/marketplace/route.ts` exits 0
    - `grep -q "requireUser" apps/web/src/app/api/marketplace/route.ts` exits 0
    - `grep -q "POST\|PATCH" apps/web/src/app/api/marketplace/route.ts` exits 0
    - `pnpm --filter web build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web build</automated>
  </verify>
  <done>Single marketplace route handles public anonymous GET and authenticated admin GET/mutations</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous → /api/marketplace GET | Public read — published items only |
| Anonymous → /api/deals, /api/leads | Must reject |
| Authenticated → admin API | Role-checked mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Elevation of Privilege | All admin API routes | mitigate | requireUser() + requireRole() at every handler entry |
| T-1-02 | Information Disclosure | marketplace GET | mitigate | Anonymous path keeps published-only filter; no service-role on public branch |
| T-1-03 | Spoofing | /api/auth/register | accept | Route migrates as-is; SEC-01 hardening deferred Phase 3 — document in SUMMARY |
</threat_model>

<verification>
- `pnpm --filter web build` passes
- `find apps/web/src/app/api -name route.ts | wc -l` ≥ 55
- Marketplace route contains both public filter and requireUser paths
- Manual: curl /api/deals without cookie → 401; curl /api/marketplace → 200
</verification>

<success_criteria>
- All admin API routes available on unified app
- Marketplace dual-GET preserves public browse and admin management
- Build and typecheck pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-app-merge/01-03-SUMMARY.md`
</output>

---
phase: 01-foundation-app-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/(site)/layout.tsx
  - apps/web/src/app/globals.css
  - apps/web/tailwind.config.ts
  - apps/web/src/app/(site)/page.tsx
  - apps/web/src/app/(site)/about/page.tsx
  - apps/web/src/app/(site)/authenticate/page.tsx
  - apps/web/src/app/(site)/consign/page.tsx
  - apps/web/src/app/(site)/contact/page.tsx
  - apps/web/src/app/(site)/marketplace/page.tsx
  - apps/web/src/app/(site)/terms-policies/page.tsx
  - apps/web/src/app/(site)/verify/page.tsx
autonomous: true
requirements:
  - FND-02
must_haves:
  truths:
    - "Public routes render with Header/Footer via (site) layout only"
    - "Root layout no longer wraps all routes in public chrome"
    - "Public URLs unchanged (/marketplace, /verify, etc.)"
    - "Admin theme CSS tokens available in globals.css for later waves"
  artifacts:
    - path: "apps/web/src/app/(site)/layout.tsx"
      provides: "Public shell — Header, Footer, Currency, Compare"
      contains: "Header"
    - path: "apps/web/src/app/layout.tsx"
      provides: "Minimal root layout — fonts, Toaster only"
      min_lines: 20
    - path: "apps/web/src/app/globals.css"
      provides: "Merged web + admin CSS variables"
  key_links:
    - from: "apps/web/src/app/(site)/layout.tsx"
      to: "@/components/shell/Header"
      via: "import and render"
      pattern: "import.*Header.*from.*@/components/shell/Header"
    - from: "apps/web/src/app/(site)/page.tsx"
      to: "apps/web/src/app/(home)/HomeContent"
      via: "re-export or import"
      pattern: "HomeContent"
---

<objective>
Tách public shell khỏi root layout bằng route group `(site)` — chuẩn bị cho admin routes không inherit Header/Footer.

Purpose: Đây là blocker đầu tiên (D-05, D-06) — hiện tại `apps/web/src/app/layout.tsx` wrap mọi route trong public chrome.
Output: `(site)/layout.tsx` với public shell; slim root layout; public routes di chuyển dưới `(site)/`; admin CSS tokens merged.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-app-merge/01-CONTEXT.md
@.planning/phases/01-foundation-app-merge/01-RESEARCH.md
@apps/web/src/app/layout.tsx
@apps/admin/src/app/globals.css

<interfaces>
From apps/web/src/app/layout.tsx (lines 121-136 pattern):
- Root currently imports Header, Footer, CompareProvider, CurrencyProvider, SpeedInsights
- Metadata export stays at root layout (D-05: minimal touch)

Routes to move under `(site)/` (NO URL segment change):
- page.tsx, about/, authenticate/, consign/, contact/, marketplace/, terms-policies/, verify/
- consign/success/ nested under consign/

Keep at app root (NOT under site):
- api/, sitemap.ts, robots.ts, manifest.ts, opengraph-image.tsx, icon.tsx, apple-icon.tsx, twitter-image.tsx
- (home)/ co-located components — import from (site)/page.tsx without moving folder
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create (site) layout and relocate public routes</name>
  <read_first>
    - apps/web/src/app/layout.tsx
    - apps/web/src/app/page.tsx
    - .planning/phases/01-foundation-app-merge/01-CONTEXT.md (D-05, D-06)
  </read_first>
  <files>
    apps/web/src/app/(site)/layout.tsx
    apps/web/src/app/layout.tsx
    apps/web/src/app/(site)/page.tsx
    apps/web/src/app/(site)/about/page.tsx
    apps/web/src/app/(site)/authenticate/page.tsx
    apps/web/src/app/(site)/consign/page.tsx
    apps/web/src/app/(site)/consign/success/page.tsx
    apps/web/src/app/(site)/contact/page.tsx
    apps/web/src/app/(site)/marketplace/page.tsx
    apps/web/src/app/(site)/marketplace/[slug]/page.tsx
    apps/web/src/app/(site)/terms-policies/page.tsx
    apps/web/src/app/(site)/verify/page.tsx
  </files>
  <action>
    Per D-05/D-06:

    1. Create `apps/web/src/app/(site)/layout.tsx` — extract public shell from root layout:
       - CompareProvider, CurrencyProvider, Header (Suspense), main, Footer, CompareDrawer, SpeedInsights
       - Copy exact structure from current root layout lines 121-136

    2. Slim `apps/web/src/app/layout.tsx`:
       - Keep: html/body, font variables (workSans, zapfRenaissance), globals.css import, metadata export, Toaster
       - Remove: Header, Footer, CompareProvider, CurrencyProvider, CompareDrawer, SpeedInsights
       - Do NOT add CommandPalette yet (Wave 2)

    3. Move public route folders to `(site)/` using `git mv` to preserve history:
       - page.tsx → (site)/page.tsx (import HomeContent from `../(home)/HomeContent`)
       - about/, authenticate/, consign/ (include success/), contact/, marketplace/ (include [slug]/), terms-policies/, verify/
       - Move co-located `components/` subfolders with their parent routes

    4. Fix relative imports in moved files if any break (absolute `@/` imports should be unchanged)

    Do NOT relocate `components/shell/`, `components/shared/`, or `(home)/` folder.
    Do NOT create admin routes in this task.
  </action>
  <acceptance_criteria>
    - `test -f apps/web/src/app/(site)/layout.tsx` exits 0
    - `grep -q "Header" apps/web/src/app/(site)/layout.tsx` exits 0
    - `! grep -q "Header" apps/web/src/app/layout.tsx` exits 0 (root must NOT import Header)
    - `test -f apps/web/src/app/(site)/marketplace/page.tsx` exits 0
    - `! test -f apps/web/src/app/marketplace/page.tsx` exits 0 (old path removed)
    - `test -f apps/web/src/app/(site)/page.tsx` exits 0
    - `grep -q "api/" apps/web/src/app/api/marketplace/route.ts` exits 0 (api stays at root)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web build</automated>
  </verify>
  <done>Public routes live under (site)/; root layout is minimal; build passes with unchanged public URLs</done>
</task>

<task type="auto">
  <name>Task 2: Merge admin CSS tokens into web globals and tailwind</name>
  <read_first>
    - apps/web/src/app/globals.css
    - apps/admin/src/app/globals.css
    - apps/web/tailwind.config.ts
    - apps/admin/tailwind.config.ts
  </read_first>
  <files>
    apps/web/src/app/globals.css
    apps/web/tailwind.config.ts
  </files>
  <action>
    Merge admin theme tokens so Wave 4 admin UI renders correctly (Pitfall 6 from RESEARCH):

    1. Read `apps/admin/src/app/globals.css` — copy admin-specific CSS variables (`--bg-0`, `--surface`, admin sidebar tokens, etc.) into `apps/web/src/app/globals.css`
    2. Merge `apps/admin/tailwind.config.ts` theme extensions into `apps/web/tailwind.config.ts` (colors, spacing aliases like `bg-bg-0`)
    3. Avoid duplicate `:root` blocks — merge into single block
    4. Do NOT change public site visual appearance — only add missing admin tokens

    shadcn-guard: do NOT edit `components/ui/**`.
  </action>
  <acceptance_criteria>
    - `grep -q "bg-bg-0\|--bg-0\|surface" apps/web/src/app/globals.css` exits 0
    - `grep -q "bg-bg-0\|surface" apps/web/tailwind.config.ts` exits 0
    - `pnpm --filter web build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web check-types</automated>
  </verify>
  <done>Admin CSS tokens present in unified app; typecheck passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public layout scope | `(site)` group must not leak to future `/admin` routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | Root layout | accept | No auth data in layout split; public routes unchanged |
</threat_model>

<verification>
- `pnpm --filter web build` passes
- `pnpm --filter web check-types` passes
- Public URLs resolve: `/`, `/marketplace`, `/verify` (route files exist under `(site)/`)
</verification>

<success_criteria>
- Root layout has no Header/Footer imports
- All 9 public route segments exist under `app/(site)/`
- Build green — foundation for admin route isolation complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-app-merge/01-01-SUMMARY.md`
</output>

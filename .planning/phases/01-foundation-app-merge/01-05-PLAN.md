---
phase: 01-foundation-app-merge
plan: 05
type: execute
wave: 5
depends_on:
  - "04"
files_modified:
  - apps/web/supabase/migrations/
  - apps/web/supabase/STORAGE_GUIDE.md
  - apps/web/supabase/MIGRATIONS.md
  - package.json
  - tsconfig.json
  - turbo.json
  - pnpm-workspace.yaml
autonomous: false
requirements:
  - FND-05
  - FND-06
user_setup:
  - service: supabase
    why: "Migrations are SQL files applied manually — Supabase CLI not in repo"
    dashboard_config:
      - task: "Apply any unapplied migrations via Supabase Dashboard SQL Editor or local Supabase CLI"
        location: "Supabase Dashboard → SQL Editor (or `supabase db push` if CLI installed locally)"
must_haves:
  truths:
    - "31 SQL migration files exist at apps/web/supabase/migrations/"
    - "MIGRATIONS.md documents manual apply instructions"
    - "pnpm dev starts single unified app (web only)"
    - "dev:admin and start:admin removed from root package.json"
    - "tsconfig.json no longer references apps/admin"
    - "pnpm check passes (lint + typecheck + build)"
  artifacts:
    - path: "apps/web/supabase/migrations/001_create_profiles.sql"
      provides: "First migration file"
    - path: "apps/web/supabase/MIGRATIONS.md"
      provides: "Manual migration apply guide"
    - path: "package.json"
      provides: "Unified dev script"
      contains: "dev:web"
  key_links:
    - from: "package.json scripts.dev"
      to: "apps/web"
      via: "--filter=web"
      pattern: "filter=web"
    - from: "apps/web/supabase/migrations/"
      to: "Supabase hosted DB"
      via: "manual SQL apply"
      pattern: "001_create_profiles"
---

<objective>
Consolidate config: migrations folder, root scripts, turbo/tsconfig — hoàn tất FND-05 và FND-06.

Purpose: Single dev entry point và single migrations source of truth. `apps/admin/` directory kept until Phase 5 CONS-02 (D-11).
Output: `apps/web/supabase/migrations/` (31 files), MIGRATIONS.md, updated root scripts, green `pnpm check`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-app-merge/01-CONTEXT.md
@.planning/phases/01-foundation-app-merge/01-RESEARCH.md
@apps/admin/supabase/STORAGE_GUIDE.md
@package.json
@tsconfig.json

<interfaces>
Migration relocation (FND-05):
- Source: apps/admin/supabase/migrations/ (31 SQL files: 001–031)
- Target: apps/web/supabase/migrations/
- Also copy: apps/admin/supabase/STORAGE_GUIDE.md
- No data migration — moving folder does not re-run applied migrations

Root script changes (FND-06):
- `dev` → `turbo run dev --filter=web` (single app)
- REMOVE: `dev:admin`, `start:admin`, `dev:all`, `start:all` (or keep dev:all as alias — prefer remove per FND-06)
- KEEP: `dev:web` as explicit alias

tsconfig.json: remove `{ "path": "./apps/admin" }` reference
pnpm-workspace.yaml: KEEP `apps/*` (D-11 — directory stays until Phase 5)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>[BLOCKING] Task 1: Consolidate Supabase migrations folder</name>
  <read_first>
    - apps/admin/supabase/migrations/001_create_profiles.sql
    - apps/admin/supabase/STORAGE_GUIDE.md
    - .planning/phases/01-foundation-app-merge/01-RESEARCH.md (Runtime State Inventory)
  </read_first>
  <files>
    apps/web/supabase/migrations/001_create_profiles.sql
    apps/web/supabase/migrations/031_add_marketplace_metadata.sql
    apps/web/supabase/STORAGE_GUIDE.md
    apps/web/supabase/MIGRATIONS.md
  </files>
  <action>
    **[BLOCKING] FND-05 — Schema folder consolidation**

    1. Create `apps/web/supabase/migrations/` directory
    2. Copy all 31 SQL files from `apps/admin/supabase/migrations/` using `git mv` or `cp` preserving filenames (001–031)
    3. Copy `apps/admin/supabase/STORAGE_GUIDE.md` → `apps/web/supabase/STORAGE_GUIDE.md`
    4. Create `apps/web/supabase/MIGRATIONS.md` documenting:
       - **Location:** `apps/web/supabase/migrations/` is canonical source (replaces `apps/admin/supabase/migrations/`)
       - **Apply method:** Supabase CLI NOT in repo — apply via:
         a) Supabase Dashboard → SQL Editor (paste migration SQL in order)
         b) Local Supabase CLI if installed: `supabase db push` from `apps/web/` (requires `supabase init` config)
       - **Important:** Moving folder does NOT re-run already-applied migrations on hosted project
       - **New environments:** Apply migrations 001→031 in numeric order
       - **Verification:** `ls apps/web/supabase/migrations/*.sql | wc -l` must equal 31
    5. Leave `apps/admin/supabase/migrations/` in place (D-11) but add comment in MIGRATIONS.md that admin path is deprecated

    Do NOT run `supabase db push` in CI — document manual steps only.
  </action>
  <acceptance_criteria>
    - `test -d apps/web/supabase/migrations` exits 0
    - `ls apps/web/supabase/migrations/*.sql 2>/dev/null | wc -l | tr -d ' ' | grep -q '^31$'` exits 0
    - `test -f apps/web/supabase/migrations/001_create_profiles.sql` exits 0
    - `test -f apps/web/supabase/migrations/031_add_marketplace_metadata.sql` exits 0
    - `test -f apps/web/supabase/MIGRATIONS.md` exits 0
    - `grep -q "apps/web/supabase/migrations" apps/web/supabase/MIGRATIONS.md` exits 0
    - `grep -q "Supabase Dashboard" apps/web/supabase/MIGRATIONS.md` exits 0
    - `test -f apps/web/supabase/STORAGE_GUIDE.md` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -d apps/web/supabase/migrations && ls apps/web/supabase/migrations/*.sql | wc -l | tr -d ' ' | grep -q '^31$'</automated>
  </verify>
  <done>31 migration files at apps/web/supabase/migrations/ with MIGRATIONS.md apply guide</done>
</task>

<task type="auto">
  <name>Task 2: Update root scripts and tsconfig for unified app</name>
  <read_first>
    - package.json
    - tsconfig.json
    - turbo.json
    - pnpm-workspace.yaml
    - .planning/phases/01-foundation-app-merge/01-CONTEXT.md (D-11, FND-06)
  </read_first>
  <files>
    package.json
    tsconfig.json
  </files>
  <action>
    Per FND-06, D-11:

    1. Update root `package.json` scripts:
       - `"dev": "turbo run dev --filter=web"` (was `turbo run dev` running both apps)
       - REMOVE: `dev:admin`, `start:admin`, `dev:all`, `start:all`
       - KEEP: `dev:web`, `start:web`, `build`, `lint`, `typecheck`, `check`

    2. Update root `tsconfig.json`:
       - Remove `{ "path": "./apps/admin" }` from references array
       - Keep: packages/shared, packages/ui, apps/web

    3. Do NOT remove `apps/admin` from `pnpm-workspace.yaml` (D-11 — kept until Phase 5 CONS-02)

    4. Do NOT delete `apps/admin/` directory

    5. Optionally update root README references to `dev:admin` → `dev` (only if README is in plan scope — skip unless build breaks)
  </action>
  <acceptance_criteria>
    - `grep -q '"dev": "turbo run dev --filter=web"' package.json` exits 0
    - `! grep -q 'dev:admin' package.json` exits 0
    - `! grep -q 'start:admin' package.json` exits 0
    - `! grep -q 'apps/admin' tsconfig.json` exits 0
    - `grep -q 'apps/web' tsconfig.json` exits 0
    - `grep -q 'apps/\*' pnpm-workspace.yaml` exits 0 (workspace still includes apps/admin dir)
  </acceptance_criteria>
  <verify>
    <automated>! grep -q 'dev:admin' package.json && grep -q 'filter=web' package.json</automated>
  </verify>
  <done>Root dev script points to web only; tsconfig excludes apps/admin reference</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 1 gate — pnpm check and smoke UAT</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 success criteria 1-5)
    - .planning/phases/01-foundation-app-merge/01-VALIDATION.md
  </read_first>
  <files>N/A — verification only</files>
  <action>
    Run full phase gate automation, then pause for human smoke UAT.
    1. Execute `pnpm check` — lint + typecheck + build all packages
    2. Verify migration count: `ls apps/web/supabase/migrations/*.sql | wc -l` = 31
    3. Verify scripts: `! grep -q dev:admin package.json`
    4. Start `pnpm dev` and confirm single app on port 1300
    5. Present ROADMAP success criteria checklist to user; wait for approval
  </action>
  <what-built>Complete Phase 1: unified app with admin at /admin, migrations consolidated, single dev entry</what-built>
  <how-to-verify>
    1. Run `pnpm check` — must pass (lint + typecheck + build)
    2. Run `pnpm dev` — single app on port 1300 (not 3600)
    3. Verify ROADMAP success criteria:
       - /admin → CRM sidebar, no public header/footer
       - /admin/deals logged out → /admin/login?redirect=...
       - /marketplace → public site works
       - /admin/leads logged in → data loads via /api/*
       - `ls apps/web/supabase/migrations/*.sql | wc -l` → 31
       - `grep dev:admin package.json` → no match
    4. Confirm `.env.local` has: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
  </how-to-verify>
  <acceptance_criteria>
    - `pnpm check` exits 0
    - `pnpm dev` starts only web app on port 1300
    - All 5 ROADMAP Phase 1 success criteria confirmed by human
  </acceptance_criteria>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <done>Phase 1 complete — all FND requirements satisfied and human UAT approved</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL files | Applied to production DB — order matters |
| Root scripts | Dev command must not start stale admin app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | Migration files | mitigate | Copy not modify; MIGRATIONS.md documents ordered apply |
| T-1-02 | Denial of Service | Wrong migration order | mitigate | Numeric 001-031 ordering documented in MIGRATIONS.md |
</threat_model>

<verification>
- `test -d apps/web/supabase/migrations && ls apps/web/supabase/migrations/*.sql | wc -l` = 31
- `! grep -q dev:admin package.json`
- `pnpm check` passes
- `pnpm dev` starts single app
</verification>

<success_criteria>
- FND-05: Migrations at apps/web/supabase/migrations/ with apply docs
- FND-06: Single dev entry, no dev:admin, tsconfig updated
- Phase 1 ROADMAP success criteria all TRUE
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-app-merge/01-05-SUMMARY.md`
</output>

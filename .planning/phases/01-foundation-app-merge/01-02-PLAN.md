---
phase: 01-foundation-app-merge
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/web/src/lib/supabase/client.ts
  - apps/web/src/lib/supabase/server.ts
  - apps/web/src/lib/supabase/middleware.ts
  - apps/web/src/lib/supabase/requireUser.ts
  - apps/web/src/lib/supabase/requireRole.ts
  - apps/web/src/lib/supabase/types.ts
  - apps/web/src/middleware.ts
  - apps/web/src/app/admin/login/page.tsx
  - apps/web/src/admin/users/pages/LoginPage.tsx
  - apps/web/package.json
autonomous: true
requirements:
  - FND-03
must_haves:
  truths:
    - "Middleware refreshes Supabase session on matched routes"
    - "Unauthenticated /admin/* redirects to /admin/login?redirect={path}"
    - "/admin/login renders without sidebar shell"
    - "Post-login always navigates to /admin (ignores redirect param per D-08)"
    - "Admin-only deps (cmdk, recharts, dnd-kit) installed in web package"
  artifacts:
    - path: "apps/web/src/middleware.ts"
      provides: "Session refresh + admin auth guard"
      exports: ["middleware", "config"]
    - path: "apps/web/src/lib/supabase/requireUser.ts"
      provides: "API route auth helper"
    - path: "apps/web/src/app/admin/login/page.tsx"
      provides: "Admin login entry at /admin/login"
    - path: "apps/web/src/lib/supabase/client.ts"
      provides: "Browser Supabase client"
  key_links:
    - from: "apps/web/src/middleware.ts"
      to: "apps/web/src/lib/supabase/middleware.ts"
      via: "updateSession import"
      pattern: "updateSession"
    - from: "apps/web/src/app/admin/login/page.tsx"
      to: "LoginPage component"
      via: "re-export"
      pattern: "LoginPage"
    - from: "apps/web/src/admin/users/pages/LoginPage.tsx"
      to: "/admin"
      via: "router.push after signIn"
      pattern: 'router\.push\(["\x27]/admin["\x27]\)'
---

<objective>
Port toàn bộ Supabase auth layer từ admin sang web và bật middleware bảo vệ `/admin/*`.

Purpose: Web hiện chỉ có `createServiceRoleClient()` — thiếu SSR client, requireUser, middleware (FND-03, D-07–D-10).
Output: `lib/supabase/*` đầy đủ, `middleware.ts`, `/admin/login` page với redirect behavior khóa.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-app-merge/01-CONTEXT.md
@.planning/phases/01-foundation-app-merge/01-RESEARCH.md
@apps/admin/src/lib/supabase/middleware.ts
@apps/admin/src/lib/supabase/server.ts
@apps/admin/src/lib/supabase/requireUser.ts
@apps/admin/src/app/login/page.tsx
@apps/web/src/lib/supabase/server.ts

<interfaces>
Admin middleware redirect (MUST CHANGE per D-07/D-09):
- OLD: `/login` with redirect param used post-login
- NEW: `/admin/login` redirect target; post-login always `/admin` (D-08)

Files to port from apps/admin/src/lib/supabase/:
- client.ts, server.ts (merge with web's createServiceRoleClient), middleware.ts, requireUser.ts, requireRole.ts, types.ts

Web server.ts currently ONLY exports createServiceRoleClient — must ADD createClient() SSR from admin without removing service role.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port Supabase auth layer to web</name>
  <read_first>
    - apps/admin/src/lib/supabase/client.ts
    - apps/admin/src/lib/supabase/server.ts
    - apps/admin/src/lib/supabase/middleware.ts
    - apps/admin/src/lib/supabase/requireUser.ts
    - apps/admin/src/lib/supabase/requireRole.ts
    - apps/admin/src/lib/supabase/types.ts
    - apps/web/src/lib/supabase/server.ts
    - apps/web/src/lib/supabase/types.ts
  </read_first>
  <files>
    apps/web/src/lib/supabase/client.ts
    apps/web/src/lib/supabase/server.ts
    apps/web/src/lib/supabase/middleware.ts
    apps/web/src/lib/supabase/requireUser.ts
    apps/web/src/lib/supabase/requireRole.ts
    apps/web/src/lib/supabase/types.ts
  </files>
  <action>
    Copy admin Supabase auth layer into `apps/web/src/lib/supabase/`:

    1. `client.ts` — copy from admin as-is (browser client factory)
    2. `server.ts` — MERGE: keep existing `createServiceRoleClient()` from web; ADD admin's `createClient()` SSR cookie client. Export both.
    3. `middleware.ts` — copy `updateSession()` from admin; UPDATE auth guard per D-09:
       ```typescript
       if (!user && pathname.startsWith("/admin") && !pathname.startsWith("/admin/login")) {
         url.pathname = "/admin/login";
         url.searchParams.set("redirect", pathname);
         return NextResponse.redirect(url);
       }
       ```
       Remove old `/login` path references.
    4. `requireUser.ts`, `requireRole.ts` — copy from admin as-is
    5. `types.ts` — use admin's generated Database types (superset of web types); replace web types.ts

    Do NOT duplicate client factories. One `lib/supabase/` directory only.
  </action>
  <acceptance_criteria>
    - `test -f apps/web/src/lib/supabase/client.ts` exits 0
    - `test -f apps/web/src/lib/supabase/requireUser.ts` exits 0
    - `grep -q "createServiceRoleClient" apps/web/src/lib/supabase/server.ts` exits 0
    - `grep -q "createClient" apps/web/src/lib/supabase/server.ts` exits 0
    - `grep -q "/admin/login" apps/web/src/lib/supabase/middleware.ts` exits 0
    - `! grep -q 'pathname = "/login"' apps/web/src/lib/supabase/middleware.ts` exits 0
    - `pnpm --filter web check-types` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web check-types</automated>
  </verify>
  <done>Full Supabase auth layer exists in web with D-09 redirect paths</done>
</task>

<task type="auto">
  <name>Task 2: Add middleware, login page, and admin deps</name>
  <read_first>
    - apps/admin/src/middleware.ts
    - apps/admin/src/app/login/page.tsx
    - apps/admin/package.json
    - .planning/phases/01-foundation-app-merge/01-CONTEXT.md (D-07, D-08)
  </read_first>
  <files>
    apps/web/src/middleware.ts
    apps/web/src/app/admin/login/page.tsx
    apps/web/src/admin/users/pages/LoginPage.tsx
    apps/web/package.json
  </files>
  <action>
    Per D-07, D-08, D-10:

    1. Create `apps/web/src/middleware.ts`:
       - Import `updateSession` from `@/lib/supabase/middleware`
       - Export `middleware` function calling `updateSession(request)`
       - Export `config.matcher` covering all routes except static assets (copy admin matcher pattern)
       - Session refresh on all matched routes; auth guard delegated to updateSession

    2. Create login flow at `/admin/login` (NOT `/login`):
       - `apps/web/src/admin/users/pages/LoginPage.tsx` — adapt from `apps/admin/src/app/login/page.tsx`
       - CRITICAL D-08: replace redirect-param navigation with `router.push("/admin")` always after successful signIn
       - Remove or disable public signup view (signup tab can stay UI-only but register is Phase 3 SEC-01)
       - `apps/web/src/app/admin/login/page.tsx` — thin re-export: `export { default } from "@/admin/users/pages/LoginPage"`
       - Login page is OUTSIDE `(portal)` group — no sidebar (sibling to future portal routes)

    3. Install admin-only deps into web:
       ```bash
       pnpm --filter web add cmdk recharts @dnd-kit/core @dnd-kit/utilities
       ```

    4. Document in commit/summary: `.env.local` must include `NEXT_PUBLIC_SUPABASE_ANON_KEY` (required for middleware). Do NOT commit secrets.

    Do NOT create `(portal)` layout yet (Wave 4).
  </action>
  <acceptance_criteria>
    - `test -f apps/web/src/middleware.ts` exits 0
    - `grep -q "updateSession" apps/web/src/middleware.ts` exits 0
    - `test -f apps/web/src/app/admin/login/page.tsx` exits 0
    - `grep -q 'router.push("/admin")' apps/web/src/admin/users/pages/LoginPage.tsx` exits 0
    - `! grep -q 'searchParams.get("redirect")' apps/web/src/admin/users/pages/LoginPage.tsx` exits 0 (no post-login redirect param usage)
    - `grep -q '"cmdk"' apps/web/package.json` exits 0
    - `pnpm --filter web build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web build</automated>
  </verify>
  <done>Middleware compiles; /admin/login route exists; D-08 redirect locked; admin deps installed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → middleware | Untrusted requests to /admin/* |
| Middleware → API routes | Middleware bypass possible (CVE class) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Elevation of Privilege | middleware.ts | mitigate | Auth guard on /admin/*; handler-level requireUser in Wave 3 |
| T-1-02 | Spoofing | LoginPage | mitigate | Supabase signInWithPassword only; no custom JWT |
| T-1-03 | Information Disclosure | middleware env skip | accept | Missing anon key allows request through; client shows error |
</threat_model>

<verification>
- `pnpm --filter web build` passes
- `pnpm --filter web check-types` passes
- Middleware file exists with `/admin/login` redirect (not `/login`)
- LoginPage uses `router.push("/admin")` post-success
</verification>

<success_criteria>
- Full Supabase auth layer ported
- Middleware protects /admin/* per D-09
- /admin/login accessible without sidebar layout
- Admin deps installed in web package.json
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-app-merge/01-02-SUMMARY.md`
</output>

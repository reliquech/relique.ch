---
phase: 11-javascript-bundle-performance-baseline-audit-tree-shakeable-
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/app/layout.tsx
  - src/components/admin/shell/AdminPortalLayout.tsx
  - src/components/command/CommandPalette.tsx
autonomous: true
requirements:
  - PERF-11-02
  - PERF-11-03
must_haves:
  truths:
    - "Public routes do not load CommandPalette/cmdk in root layout"
    - "Admin portal routes retain Cmd+K palette"
  artifacts:
    - path: src/app/layout.tsx
      provides: "Root layout without CommandPalette"
    - path: src/components/admin/shell/AdminPortalLayout.tsx
      provides: "CommandPalette mounted for admin portal only"
---

<objective>
Wave 2: Remove highest-impact bundle leak — admin CommandPalette from root layout.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11-javascript-bundle-performance-baseline-audit-tree-shakeable-/11-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate CommandPalette to admin portal</name>
  <files>src/app/layout.tsx, src/components/admin/shell/AdminPortalLayout.tsx</files>
  <read_first>
    - src/app/layout.tsx
    - src/components/admin/shell/AdminPortalLayout.tsx
    - src/components/command/CommandPalette.tsx
    - src/app/admin/(portal)/layout.tsx
  </read_first>
  <action>
Remove from `src/app/layout.tsx`:
- `import { CommandPalette } from "@/components/command/CommandPalette"`
- `<CommandPalette />` inside body

Add to `AdminPortalLayout.tsx` (after main content, before closing fragment/wrapper):
```tsx
import { CommandPalette } from "@/components/command/CommandPalette";
// ...
<CommandPalette />
```

Do NOT change CommandPalette behavior, keyboard shortcut, or actionRegistry logic.

`/admin/login` is outside `(portal)` layout — login page will not show palette (acceptable; palette is for authenticated CRM navigation).

Keep `<Toaster />` in root layout (needed globally for sonner toasts on public forms).
  </action>
  <acceptance_criteria>
    - `src/app/layout.tsx` does not contain string `CommandPalette`
    - `src/components/admin/shell/AdminPortalLayout.tsx` contains `<CommandPalette />`
    - `npm run check-types` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <done>cmdk bundle scoped to admin portal routes</done>
</task>

</tasks>

<verification>
- [ ] grep `CommandPalette` in src/app/layout.tsx → 0 matches
- [ ] AdminPortalLayout renders CommandPalette
</verification>

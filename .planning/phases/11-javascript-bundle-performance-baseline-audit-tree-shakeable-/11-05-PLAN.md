---
phase: 11-javascript-bundle-performance-baseline-audit-tree-shakeable-
plan: 05
type: execute
wave: 4
depends_on:
  - "03"
  - "04"
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - PERF-11-02
  - PERF-11-03
must_haves:
  truths:
    - "Only confirmed-unused dependencies removed"
    - "npm run build passes after cleanup"
  artifacts:
    - path: package.json
      provides: "No duplicate motion package if unused"
---

<objective>
Wave 4: Remove confirmed-unused dependency (`motion`) and document any deferred dep removals.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11-javascript-bundle-performance-baseline-audit-tree-shakeable-/11-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused motion package</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json
    - .planning/phases/11-javascript-bundle-performance-baseline-audit-tree-shakeable-/11-AUDIT.md
  </read_first>
  <action>
Verify zero imports:
```bash
rg 'from ["\']motion["\']' src/
```
Expected: 0 matches (project uses `framer-motion` only).

If confirmed:
```bash
npm uninstall motion
```

Do NOT remove `framer-motion` — used in 40+ files.

Do NOT remove `@dnd-kit/*`, `cmdk`, `recharts` — still used after Plans 02–04.

Append to `11-AUDIT.md` section "Deferred" any dependency that had imports or uncertain usage.
  </action>
  <acceptance_criteria>
    - `rg 'from ["\']motion["\']' src/` returns 0 matches
    - `package.json` does not list `"motion"` in dependencies
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Duplicate motion package removed if unused</done>
</task>

</tasks>

<verification>
- [ ] package.json dependencies unchanged except motion removal
</verification>

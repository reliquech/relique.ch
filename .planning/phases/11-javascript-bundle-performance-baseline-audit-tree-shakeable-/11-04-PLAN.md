---
phase: 11-javascript-bundle-performance-baseline-audit-tree-shakeable-
plan: 04
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/app/(home)/HomeContent.tsx
  - src/app/(home)/components/HomeBelowFoldSections.tsx
autonomous: true
requirements:
  - PERF-11-02
  - PERF-11-03
must_haves:
  truths:
    - "Below-fold home sections code-split from initial home bundle"
    - "Hero and above-fold sections still SSR"
  artifacts:
    - path: src/app/(home)/components/HomeBelowFoldSections.tsx
      provides: "Client wrapper for lazy sections OR dynamic imports in HomeContent"
---

<objective>
Wave 3: Code-split below-fold home page sections that import framer-motion.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11-javascript-bundle-performance-baseline-audit-tree-shakeable-/11-AUDIT.md
@src/app/(home)/HomeContent.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dynamic import below-fold home sections</name>
  <files>src/app/(home)/HomeContent.tsx</files>
  <read_first>
    - src/app/(home)/HomeContent.tsx
    - src/app/(home)/components/TheWaySection.tsx
    - src/app/(home)/components/TeamSection.tsx
    - src/app/(home)/components/PressCoverageSection.tsx
  </read_first>
  <action>
In `HomeContent.tsx` (Server Component — no `"use client"`):

Keep **static** imports (above-fold / LCP-critical):
- `HeroSection`
- `WhySection`
- `MarketplaceSection`
- `DualBlocks`

Use `next/dynamic` with `ssr: true` for below-fold sections:
- `TheWaySection`
- `PartnersSection`
- `StrategicPartnerSection`
- `TeamSection`
- `PressCoverageSection` — pass `items={PRESS_ARTICLES}` prop via wrapper or dynamic child

Pattern:
```tsx
import dynamic from "next/dynamic";

const TheWaySection = dynamic(
  () => import("./components/TheWaySection").then((m) => ({ default: m.TheWaySection })),
  { loading: () => <div className="min-h-[120px]" aria-hidden /> }
);
```

Remove unused import: `TestimonialsSection` (dead code — never rendered).

Do NOT change section markup, animations, or order in the JSX tree.
  </action>
  <acceptance_criteria>
    - `HomeContent.tsx` does not import `TestimonialsSection`
    - `HomeContent.tsx` contains `import dynamic from "next/dynamic"`
    - `HeroSection` remains a static import (not dynamic)
    - `TheWaySection` loaded via dynamic()
    - `npm run check-types` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <done>Home below-fold framer-motion sections code-split</done>
</task>

</tasks>

<verification>
- [ ] Home page build includes separate async chunks for lazy sections
- [ ] `/` renders without hydration errors (manual smoke in 11-06)
</verification>

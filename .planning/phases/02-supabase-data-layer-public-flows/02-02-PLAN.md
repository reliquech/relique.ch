---
phase: 02-supabase-data-layer-public-flows
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - .planning/phases/02-supabase-data-layer-public-flows/02-UAT-CHECKLIST.md
  - scripts/phase2-public-smoke.mjs
  - src/app/api/public/verify/route.ts
  - src/app/api/public/consign/route.ts
  - src/app/api/public/contact/route.ts
  - src/lib/verify/lookupCode.ts
  - src/lib/verify/mapMarketplaceToResult.ts
  - src/components/results/VerifyResultDisplay.tsx
  - src/app/(site)/verify/page.tsx
  - src/app/(site)/consign/components/ConsignForm.tsx
  - src/app/(site)/contact/components/ContactForm.tsx
  - src/admin/submissions/pages/SubmissionsPage.tsx
autonomous: false
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - VRFY-01
  - VRFY-02
  - VRFY-03
  - VRFY-04
  - CNSG-01
  - CNSG-02
  - CNSG-03
  - CNSG-04
  - CNSG-05
  - CNTC-01
  - CNTC-02
  - CNTC-03
user_setup:
  - service: supabase
    why: "Public flows persist to PostgreSQL; migrations 032–034 must be applied"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard → Project Settings → API"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project Settings → API"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API (server only)"
    dashboard_config:
      - task: "Confirm migrations 032–034 applied (product_id, consign-submissions bucket)"
        location: "Supabase Dashboard → Database → Migrations or SQL editor"
      - task: "Seed one marketplace_items row with product_id for verify UAT"
        location: "Supabase Dashboard → Table Editor → marketplace_items"
must_haves:
  truths:
    - "GET /api/public/verify?code= returns found:true with VerifyResult from DB for seeded code"
    - "GET /api/public/verify?code=UNKNOWN returns found:false (VRFY-01/D-04)"
    - "POST /api/public/consign with FormData creates consigned_items + lead + attachments"
    - "POST /api/public/contact creates leads + messages rows"
    - "Consign redirects to /consign/success; contact shows inline success (no alert)"
    - "CNSG-03/CNTC-02 satisfied via on-screen confirmation + CRM — no email sent"
  artifacts:
    - path: .planning/phases/02-supabase-data-layer-public-flows/02-UAT-CHECKLIST.md
      provides: "Requirement-mapped UAT steps"
    - path: scripts/phase2-public-smoke.mjs
      provides: "Optional HTTP smoke against localhost:1300"
    - path: src/app/api/public/verify/route.ts
      provides: "Public verify lookup"
      exports: ["GET"]
    - path: src/app/api/public/consign/route.ts
      provides: "Public consign multipart submit"
      exports: ["POST"]
    - path: src/app/api/public/contact/route.ts
      provides: "Public contact JSON submit"
      exports: ["POST"]
  key_links:
    - from: src/app/(site)/verify/page.tsx
      to: /api/public/verify
      via: verifyService → verify.supabase.ts fetch
      pattern: "api/public/verify"
    - from: src/app/(site)/consign/components/ConsignForm.tsx
      to: /api/public/consign
      via: submitPublicConsign FormData POST
      pattern: "api/public/consign"
    - from: src/app/(site)/contact/components/ContactForm.tsx
      to: /api/public/contact
      via: fetch POST JSON
      pattern: "api/public/contact"
---

<objective>
Validate verify, consign, and contact public flows against all Phase 2 requirements (except ADM-04); fix any gaps found; produce a UAT checklist for human sign-off.

Purpose: Code complete — this plan proves DATA-01–03, VRFY-01–04, CNSG-01–05, CNTC-01–03 work end-to-end with Supabase.
Output: `02-UAT-CHECKLIST.md`, smoke script, gap fixes, human UAT checkpoint passed.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-supabase-data-layer-public-flows/02-CONTEXT.md
@.planning/phases/02-supabase-data-layer-public-flows/02-RESEARCH.md

<interfaces>
<!-- Key contracts — executor uses directly -->

From `src/app/api/public/verify/route.ts`:
- GET `?code=` → `{ found: false }` | `{ found: true, result: VerifyResult }`
- Uses `createAnonClient()` + `lookupMarketplaceItemByCode` + `mapMarketplaceToVerifyResult`

From `src/app/api/public/consign/route.ts`:
- POST multipart: fields `contact_name`, `contact_email`, `contact_phone?`, `item_description`, `category?`, `coa_issuer?`, `photos` (File[]), honeypot `website`
- Returns 201 `{ id, status: "submitted" }`; creates `consigned_items`, `leads` (source=consign), storage uploads to `consign-submissions`, `attachments`

From `src/app/api/public/contact/route.ts`:
- POST JSON `{ name, email, message, website? }` → 201 `{ id }`; creates `leads` (source=contact) + `messages`

From `src/lib/verify/mapMarketplaceToResult.ts`:
- Maps `marketplaceSlug` / `marketplaceUrl` only when `state_lifecycle=published` + public visibility (VRFY-04, D-05)

CNSG-03 / CNTC-02 / CNTC-03 (no email — D-20/D-21):
- CNSG-03 → `/consign/success` redirect + toast
- CNTC-02 → operator sees lead/message in `/admin` CRM (no inbox email)
- CNTC-03 → ContactForm inline success state (already implemented)
</interfaces>

## Pre-check: migrations

Migrations **032–034 already exist** at `supabase/migrations/`. Do **NOT** create new migrations unless smoke proves a column/bucket is missing on the linked project. If missing, document [BLOCKING] apply via Dashboard SQL — not `db push` unless user confirms CLI is linked.

## Verify seed guidance

For UAT, ensure at least one `marketplace_items` row has:
- `product_id = 'RLQ-TEST-0001'` (or similar)
- `auth.status = 'verified'`, populated `listing`, `signing`, `condition`, `media` JSONB
- Optional: `state_lifecycle = 'published'` + slug for VRFY-04 link test
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UAT checklist and public-flow smoke script</name>
  <files>.planning/phases/02-supabase-data-layer-public-flows/02-UAT-CHECKLIST.md, scripts/phase2-public-smoke.mjs, package.json</files>
  <action>
    Create `02-UAT-CHECKLIST.md` mapping each requirement to concrete steps:

    | Req | Step |
    |-----|------|
    | VRFY-01 | `/verify?code=RLQ-TEST-0001` → qualified/inconclusive/disqualified from DB |
    | VRFY-02 | `/verify?code=RLQ-TEST-0001` via URL param auto-runs (page useEffect) |
    | VRFY-03 | Result panel shows signer, grade, hero image when present in JSONB |
    | VRFY-04 | Published item shows link to `/marketplace/[slug]` |
    | VRFY-01/D-04 | Unknown code → VerifyNotFoundState |
    | CNSG-01 | Submit with 1+ photos → row in consigned_items + attachments |
    | CNSG-02 | Redirect `/consign/success?id=...` |
    | CNSG-03 | On-screen success only — **no email** (D-21) |
    | CNSG-04 | Admin `/admin/submissions?tab=consignments` shows `submitted` item |
    | CNSG-05 | `leads.source = 'consign'` row created |
    | CNTC-01 | `messages` + `leads` rows with source=contact |
    | CNTC-02 | Operator views new lead in admin CRM — **no email** (D-20) |
    | CNTC-03 | Inline success panel, no `alert()` |
    | DATA-01–03 | All three flows hit Supabase, not localStorage |

    Create `scripts/phase2-public-smoke.mjs`:
    - Requires env: `PHASE2_SMOKE_BASE=http://localhost:1300`, optional `PHASE2_VERIFY_CODE=RLQ-TEST-0001`
    - Tests: GET verify (known + unknown code), POST contact JSON, POST consign with minimal 1x1 PNG buffer as `photos`
    - Skip gracefully with exit 0 + message if base URL unreachable (dev server not running)
    - Exit 1 only on HTTP 5xx or unexpected 4xx when server is up

    Add `"phase2:smoke": "node scripts/phase2-public-smoke.mjs"` to package.json
  </action>
  <verify>
    <automated>node --check scripts/phase2-public-smoke.mjs</automated>
  </verify>
  <done>Checklist covers all 16 in-scope requirement IDs; smoke script syntax-valid</done>
</task>

<task type="auto">
  <name>Task 2: Run smoke, inspect flows, fix gaps</name>
  <files>src/app/api/public/*.ts, src/lib/verify/*.ts, src/components/results/VerifyResultDisplay.tsx, src/admin/submissions/pages/SubmissionsPage.tsx</files>
  <action>
    With dev server running (`npm run dev`) and Supabase env configured:

    1. Run `npm run phase2:smoke` — fix any failures
    2. Code-review each requirement against implementation:

    **Verify (VRFY-01–04, DATA-01):**
    - Confirm `lookupCode.ts` finds by `product_id` AND `auth.coa_refs` (D-02)
    - Confirm verify page parses `?code=` without mock random generation (VRFY-02)
    - Confirm `VerifyResultDisplay` renders extended fields: `signerNames`, `grade`, `heroImageUrl`, `marketplaceUrl` (VRFY-03/04)
    - Admin verify tab: `SubmissionsPage` verifications tab must show empty/placeholder per D-08 — wire to `VerifyPage.tsx` if tab still reads localStorage

    **Consign (CNSG-01–05, DATA-02):**
    - Confirm honeypot `website` field rejects bots
    - Confirm photo validation (min 1, max 10, 10MB, mime types) per D-16/D-17
    - Confirm lead insert uses `source: 'consign'` (non-blocking log on failure is OK but should not silently drop on valid data)
    - **No email calls** in consign route (D-21)

    **Contact (CNTC-01–03, DATA-03):**
    - Confirm no `alert()` in ContactForm
    - Confirm messages linked to lead_id
    - **No email calls** in contact route (D-20)

    Fix only gaps found — minimal diffs. Do NOT add Resend. Do NOT add migrations unless 032–034 provably unapplied (then document SQL apply steps in checklist, not new DDL files).
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <done>Smoke passes when dev+Supabase available; all identified gaps fixed; typecheck clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human UAT sign-off for public flows</name>
  <files>.planning/phases/02-supabase-data-layer-public-flows/02-UAT-CHECKLIST.md</files>
  <action>Pause for human to execute every step in 02-UAT-CHECKLIST.md against running dev server + Supabase project.</action>
  <verify>
    <automated>MISSING — manual browser UAT per 02-UAT-CHECKLIST.md</automated>
  </verify>
  <done>User confirms all checklist items pass or reports specific failures</done>
  <what-built>Public verify/consign/contact flows with UAT checklist and automated smoke script</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Follow every step in `02-UAT-CHECKLIST.md`
    3. Verify page: test seeded RLQ code + unknown code + published marketplace link
    4. Consign: submit with 1+ photos → success page → check admin consignments tab + Supabase `consigned_items`/`leads`/`attachments`
    5. Contact: submit → inline success → check admin leads/messages
    6. Confirm **no emails** sent (CNSG-03, CNTC-02, ADM-04 out of scope)
  </how-to-verify>
  <resume-signal>Type "approved" or list failing checklist items</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/public/* | Unauthenticated untrusted input |
| API → Supabase service role | Server-side writes bypass RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-03 | Tampering | /api/public/consign file upload | mitigate | MIME allowlist + size limits + private bucket (D-17) |
| T-2-04 | Spoofing | Public form spam | mitigate | Honeypot `website` field reject (already in routes) |
| T-2-05 | Tampering | Verify code injection | mitigate | Parameterized Supabase queries via JS client; no raw SQL |
| T-2-06 | Denial of Service | Large multipart body | accept | maxDuration=60; document Vercel limits; full hardening Phase 3 |
</threat_model>

<verification>
```bash
npm run check-types
npm run phase2:smoke   # with dev server + .env.local
# Manual: complete 02-UAT-CHECKLIST.md
```
</verification>

<success_criteria>
- All 16 in-scope requirement IDs verified via checklist
- Gap fixes committed; typecheck passes
- Human UAT checkpoint approved
- Zero email sends for consign/contact
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-data-layer-public-flows/02-02-SUMMARY.md`
</output>

---
phase: 02-supabase-data-layer-public-flows
plan: 04
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/web/src/app/api/public/contact/route.ts
  - apps/web/src/app/(site)/contact/components/ContactForm.tsx
autonomous: true
requirements:
  - DATA-03
  - CNTC-01
  - CNTC-02
  - CNTC-03
  - ADM-04
must_haves:
  truths:
    - "POST /api/public/contact persists messages + leads"
    - "Operator receives email at OPERATOR_EMAIL env"
    - "User receives confirmation email"
    - "ContactForm shows inline success — no alert() or setTimeout fake"
  artifacts:
    - path: "apps/web/src/app/api/public/contact/route.ts"
      provides: "Public contact endpoint"
    - path: "apps/web/src/app/(site)/contact/components/ContactForm.tsx"
      provides: "Wired contact form with success state"
  key_links:
    - from: "apps/web/src/app/(site)/contact/components/ContactForm.tsx"
      to: "/api/public/contact"
      via: "fetch POST JSON"
      pattern: "api/public/contact"
    - from: "apps/web/src/app/api/public/contact/route.ts"
      to: "messages"
      via: "service role insert"
      pattern: "from(\"messages\")"
---

<objective>
Wire contact form tới Supabase CRM và transactional emails.

Purpose: Thay fake setTimeout/alert success (DATA-03, CNTC-01–03).
Output: `/api/public/contact`, ContactForm với inline success state.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-supabase-data-layer-public-flows/02-CONTEXT.md
@.planning/phases/02-supabase-data-layer-public-flows/02-RESEARCH.md
@apps/web/src/app/(site)/contact/components/ContactForm.tsx
@apps/web/supabase/migrations/010_create_crm_core.sql
@apps/web/src/lib/email/sendTransactional.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Public contact API route</name>
  <read_first>
    - apps/web/supabase/migrations/010_create_crm_core.sql (messages, leads)
    - apps/web/src/lib/email/sendTransactional.ts
    - apps/web/src/app/api/public/consign/route.ts (public route pattern)
    - .planning/phases/02-supabase-data-layer-public-flows/02-CONTEXT.md (D-19, D-20, D-21)
  </read_first>
  <files>
    apps/web/src/app/api/public/contact/route.ts
  </files>
  <action>
    POST `/api/public/contact` — no requireUser:

    Zod schema:
    - name: string min 2
    - email: email
    - message: string min 10
    - website?: string (honeypot — reject 400 if non-empty)

    Flow:
    1. Insert `leads`: full_name=name, email, source=`contact`, status=`new`
    2. Insert `messages`: name, email, message, status=`new`, source=`contact`, lead_id=lead.id
    3. sendTransactionalEmail to getOperatorEmail() — subject "New contact inquiry from {name}"
    4. sendTransactionalEmail to submitter — confirmation "We received your message"
    5. Return 201 `{ id: message.id }`

    Use sendTransactionalEmail for both emails (ADM-04).
  </action>
  <acceptance_criteria>
    - `test -f apps/web/src/app/api/public/contact/route.ts` exits 0
    - `grep -q "from(\"messages\")" apps/web/src/app/api/public/contact/route.ts` exits 0
    - `grep -q "from(\"leads\")" apps/web/src/app/api/public/contact/route.ts` exits 0
    - `grep -q "getOperatorEmail" apps/web/src/app/api/public/contact/route.ts` exits 0
    - `grep -q "sendTransactionalEmail" apps/web/src/app/api/public/contact/route.ts` exits 0
    - `! grep -q "requireUser" apps/web/src/app/api/public/contact/route.ts` exits 0
    - `pnpm --filter web check-types` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web check-types</automated>
  </verify>
  <done>Contact API persists CRM records and sends emails</done>
</task>

<task type="auto">
  <name>Task 2: Wire ContactForm with inline success state</name>
  <read_first>
    - apps/web/src/app/(site)/contact/components/ContactForm.tsx
    - .planning/phases/02-supabase-data-layer-public-flows/02-CONTEXT.md (D-22, D-23)
  </read_first>
  <files>
    apps/web/src/app/(site)/contact/components/ContactForm.tsx
  </files>
  <action>
    Refactor ContactForm (minimal UI touch):

    1. Add controlled state for name, email, message fields (useState)
    2. Add hidden honeypot input `website` (display:none, tabIndex=-1)
    3. On submit: POST JSON to `/api/public/contact`
    4. Remove `setTimeout` and `alert()` entirely
    5. On success: set `submitted=true` — show inline success message in same card styling (match site typography — uppercase tracking, primaryBlue accent)
    6. On error: show inline error text
    7. Keep existing input CSS classes — no layout redesign

    File under 300 lines.
  </action>
  <acceptance_criteria>
    - `! grep -q "setTimeout" apps/web/src/app/(site)/contact/components/ContactForm.tsx` exits 0
    - `! grep -q "alert(" apps/web/src/app/(site)/contact/components/ContactForm.tsx` exits 0
    - `grep -q "api/public/contact" apps/web/src/app/(site)/contact/components/ContactForm.tsx` exits 0
    - `grep -q "submitted" apps/web/src/app/(site)/contact/components/ContactForm.tsx` exits 0
    - `pnpm --filter web check-types` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web check-types</automated>
  </verify>
  <done>Contact form submits to real API with inline confirmation</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-05 | Denial of Service | contact route | accept | Honeypot only v1; rate limit Phase 3 |
| T-2-08 | Spoofing | contact email | mitigate | Zod email validation; operator email from env only |
</threat_model>

<verification>
- `pnpm --filter web build` passes
- No alert/setTimeout in ContactForm
- Manual: contact submit creates messages + leads rows
</verification>

<success_criteria>
- CNTC-01, CNTC-02, CNTC-03 satisfied
- DATA-03 complete (no fake success)
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-data-layer-public-flows/02-04-SUMMARY.md`
</output>

---
phase: 02-supabase-data-layer-public-flows
plan: 05
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
  - "04"
files_modified:
  - apps/web/src/lib/services/impl/verify.local.ts
  - apps/web/src/lib/services/impl/consign.local.ts
  - apps/web/src/lib/services/verifyService.ts
  - apps/web/src/lib/services/consignService.ts
  - apps/web/src/admin/submissions/pages/SubmissionsPage.tsx
  - apps/web/.env.example
autonomous: true
requirements:
  - DATA-04
must_haves:
  truths:
    - "No production code imports verify.local or consign.local"
    - "impl/index.ts only exports supabase adapters for verify/consign"
    - "verifyService.history returns empty (no localStorage reads)"
    - "pnpm --filter web build passes"
  artifacts:
    - path: "apps/web/src/lib/services/impl/index.ts"
      provides: "Production adapter exports — supabase only"
  key_links:
    - from: "apps/web/src/lib/services/impl/index.ts"
      to: "verify.supabase.ts"
      via: "export"
      pattern: "verify.supabase"
---

<objective>
Cleanup localStorage adapters và quality gate — hoàn tất DATA-04.

Purpose: Xóa mock/localStorage paths sau khi verify/consign/contact flows live.
Output: Deleted or unreferenced local adapters, env docs, passing build.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-supabase-data-layer-public-flows/02-CONTEXT.md
@.planning/phases/02-supabase-data-layer-public-flows/02-RESEARCH.md
@apps/web/src/lib/services/impl/index.ts
@apps/web/src/lib/storage.ts
@apps/web/src/admin/submissions/pages/SubmissionsPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove local adapters and dead localStorage verify/consign paths</name>
  <read_first>
    - apps/web/src/lib/services/impl/verify.local.ts
    - apps/web/src/lib/services/impl/consign.local.ts
    - apps/web/src/lib/services/impl/index.ts
    - apps/web/src/lib/services/verifyService.ts
    - apps/web/src/lib/services/consignService.ts
    - apps/web/src/admin/submissions/pages/SubmissionsPage.tsx
  </read_first>
  <files>
    apps/web/src/lib/services/impl/verify.local.ts
    apps/web/src/lib/services/impl/consign.local.ts
    apps/web/src/lib/services/verifyService.ts
    apps/web/src/lib/services/consignService.ts
    apps/web/src/admin/submissions/pages/SubmissionsPage.tsx
  </files>
  <action>
    1. Delete `verify.local.ts` and `consign.local.ts` from impl/
    2. Confirm `impl/index.ts` exports only verify.supabase + consign.supabase
    3. Update verifyService.ts history methods to return empty/no-op without calling deleted impl methods (or simplify to match verify.supabase no-op history)
    4. Update consignService.ts — remove draft/localStorage delegation; public methods used by ConsignForm go through consign.supabase
    5. SubmissionsPage.tsx: remove any verify history from localStorage in combined submissions view (if still references verifyService.history)
    6. Grep codebase — fix any remaining imports of verify.local or consign.local

    Do NOT remove VERIFY_HISTORY/CONSIGN_* keys from storage.ts yet if other legacy code references — but ensure verify/consign production paths do not call them.

    Keep marketplace.local, content.local unchanged (out of Phase 2 scope).
  </action>
  <acceptance_criteria>
    - `! test -f apps/web/src/lib/services/impl/verify.local.ts` exits 0
    - `! test -f apps/web/src/lib/services/impl/consign.local.ts` exits 0
    - `rg "verify\.local" apps/web/src --glob "*.ts" --glob "*.tsx"` returns 0 matches
    - `rg "consign\.local" apps/web/src --glob "*.ts" --glob "*.tsx"` returns 0 matches
    - `grep -q "verify.supabase" apps/web/src/lib/services/impl/index.ts` exits 0
    - `grep -q "consign.supabase" apps/web/src/lib/services/impl/index.ts` exits 0
    - `pnpm --filter web check-types` exits 0
  </acceptance_criteria>
  <verify>
    <automated>rg "verify\.local|consign\.local" apps/web/src || true; test $? -eq 1</automated>
  </verify>
  <done>Local adapters removed; no imports remain</done>
</task>

<task type="auto">
  <name>Task 2: Document env vars + phase quality gate</name>
  <read_first>
    - apps/web/.env.example
    - .planning/phases/02-supabase-data-layer-public-flows/02-VALIDATION.md
  </read_first>
  <files>
    apps/web/.env.example
  </files>
  <action>
    Update apps/web/.env.example (or create if missing) with Phase 2 vars:
    - OPERATOR_EMAIL=ops@relique.co
    - RESEND_API_KEY= (existing if present)
    - RESEND_FROM_EMAIL=support@relique.co

    Run quality gate:
    ```bash
    pnpm --filter web check-types
    pnpm --filter web build
    ```

    Document manual UAT checklist in SUMMARY:
    - /verify with known code
    - /consign with photos
    - /contact submit
    - Admin consign queue
  </action>
  <acceptance_criteria>
    - `grep -q "OPERATOR_EMAIL" apps/web/.env.example` exits 0
    - `pnpm --filter web check-types` exits 0
    - `pnpm --filter web build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web build</automated>
  </verify>
  <done>Env documented; build passes — Phase 2 code complete</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | cleanup only | accept | No new attack surface |
</threat_model>

<verification>
- `pnpm --filter web build` passes
- Zero rg matches for verify.local / consign.local in apps/web/src
- All 17 phase requirement IDs covered across plans 01–05
</verification>

<success_criteria>
- DATA-04 complete
- Phase 2 ready for /gsd-execute-phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-data-layer-public-flows/02-05-SUMMARY.md`
</output>

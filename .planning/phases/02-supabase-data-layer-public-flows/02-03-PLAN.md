---
phase: 02-supabase-data-layer-public-flows
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scripts/phase2-data-layer-gate.mjs
  - package.json
  - src/lib/services/impl/index.ts
  - src/lib/domain/storage/verify.ts
  - src/lib/domain/storage/consign.ts
  - src/lib/legacy/impl/index.ts
autonomous: true
requirements:
  - DATA-04
must_haves:
  truths:
    - "Public verify/consign/contact paths import supabase adapters only — not verify.local/consign.local"
    - "src/lib/services/impl/index.ts exports verifyServiceSupabase and consignServiceSupabase"
    - "Production verify/consign localStorage helpers are not imported outside lib/legacy/"
    - "Automated gate fails if verify.local or consign.local imported from non-legacy paths"
  artifacts:
    - path: scripts/phase2-data-layer-gate.mjs
      provides: "localStorage adapter import gate"
    - path: src/lib/services/impl/index.ts
      provides: "Active service adapter exports"
      contains: "verifyServiceSupabase"
  key_links:
    - from: src/lib/services/verifyService.ts
      to: src/lib/services/impl/index.ts
      via: "import { verifyService as verifyServiceImpl }"
      pattern: "from \"./impl\""
    - from: src/app/(site)/consign/components/ConsignForm.tsx
      to: submitPublicConsign
      via: "@/lib/services/impl/consign.supabase"
      pattern: "consign.supabase"
---

<objective>
Prove DATA-04: production public flows no longer use localStorage verify/consign adapters; quarantine legacy local impls; add automated import gate.

Purpose: D-07/D-10 mandate no browser-only verify history or consign drafts on public paths.
Output: `phase2:data-layer-gate` script; deprecated markers on storage helpers; zero non-legacy imports of `.local` adapters.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-supabase-data-layer-public-flows/02-CONTEXT.md

## Current state (verified)

- `src/lib/services/impl/index.ts` already exports `verifyServiceSupabase` + `consignServiceSupabase`
- `verify.local.ts` / `consign.local.ts` live under `src/lib/legacy/impl/` — used only by admin dashboard legacy widgets
- `src/lib/domain/storage/verify.ts` and `consign.ts` — localStorage helpers imported only by legacy impl
- Public ConsignForm imports `submitPublicConsign` from `consign.supabase.ts` directly (not local adapter)

## Phase 2 vs Phase 4 boundary

- **Phase 2 (DATA-04):** Public production paths must not use localStorage for verify/consign **data**
- **Phase 4 (CONS-03):** Delete entire `src/lib/legacy/` — do NOT delete legacy folder in this plan unless grep proves zero imports (dashboard still uses it)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map import graph and mark deprecated storage helpers</name>
  <files>src/lib/domain/storage/verify.ts, src/lib/domain/storage/consign.ts, src/lib/legacy/impl/index.ts</files>
  <action>
    Run import analysis:

    ```bash
    rg "verify\.local|consign\.local|domain/storage/verify|domain/storage/consign" src/ --glob "*.ts" --glob "*.tsx"
    ```

    Expected allowed importers of `.local` adapters:
    - `src/lib/legacy/**` only

    Expected allowed importers of `domain/storage/verify|consign`:
    - `src/lib/legacy/**` only

    Add module-level `@deprecated` JSDoc to:
    - `src/lib/domain/storage/verify.ts` — "Legacy browser storage; public verify uses Supabase (Phase 2). Removed in Phase 4."
    - `src/lib/domain/storage/consign.ts` — same pattern

    Add comment header to `src/lib/legacy/impl/index.ts` clarifying these are **admin dashboard legacy only** — not used by public site.

    Update `src/lib/services/impl/index.ts` header comment (if not done in Plan 01): production adapters are Supabase/API-only.

    If any non-legacy file imports verify.local/consign.local or domain storage helpers, refactor that file to use supabase impl or admin API — minimal fix only.
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <done>Import graph documented; only legacy/ imports local adapters; deprecated JSDoc added</done>
</task>

<task type="auto">
  <name>Task 2: Add DATA-04 automated import gate</name>
  <files>scripts/phase2-data-layer-gate.mjs, package.json</files>
  <action>
    Create `scripts/phase2-data-layer-gate.mjs`:

    **Rule 1 — forbidden imports outside legacy:**
    Scan all `src/**/*.{ts,tsx}` except paths matching `src/lib/legacy/**`:
    - Fail if file contains `verify.local` or `consign.local`
    - Fail if file imports `@/lib/domain/storage/verify` or `@/lib/domain/storage/consign`

    **Rule 2 — impl index must export supabase:**
    Fail if `src/lib/services/impl/index.ts` does not contain `verifyServiceSupabase` and `consignServiceSupabase`

    **Rule 3 — public forms must not import legacy:**
    Fail if `ConsignForm.tsx` or verify page imports from `@/lib/legacy`

    Print violations with file paths; exit 1 on any fail.

    Add to package.json:
    `"phase2:data-layer-gate": "node scripts/phase2-data-layer-gate.mjs"`
  </action>
  <verify>
    <automated>npm run phase2:data-layer-gate</automated>
  </verify>
  <done>Gate script passes on current codebase; npm script registered</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage | Must not hold authoritative verify/consign production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-07 | Information Disclosure | Verify history in localStorage | mitigate | Supabase adapter returns empty history; storage helpers deprecated + import gate |
| T-2-08 | Tampering | Consign draft localStorage | mitigate | Public flow uses single-shot API submit; draft methods return unsupported |
</threat_model>

<verification>
```bash
npm run phase2:data-layer-gate
rg "verify\.local|consign\.local" src/ --glob "!src/lib/legacy/**"
# Expect zero matches outside legacy
```
</verification>

<success_criteria>
- DATA-04 satisfied: no production path imports local verify/consign adapters
- Automated gate prevents regression
- Legacy folder quarantined with clear comments until Phase 4
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-data-layer-public-flows/02-03-SUMMARY.md`
</output>

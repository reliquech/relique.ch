---
phase: 02-supabase-data-layer-public-flows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase2-no-email-gate.mjs
  - package.json
  - .env.example
  - README.md
  - src/lib/services/impl/index.ts
autonomous: true
requirements:
  - DATA-04
user_setup: []
must_haves:
  truths:
    - "No Resend, sendTransactional, or RESEND_* references remain in src/"
    - "No OPERATOR_EMAIL env var documented as required for Phase 2"
    - "Automated grep gate fails CI-local if email code reappears in src/"
  artifacts:
    - path: scripts/phase2-no-email-gate.mjs
      provides: "Automated no-email grep gate"
      exports: ["default"]
    - path: package.json
      provides: "phase2:no-email-gate npm script"
      contains: "phase2:no-email-gate"
  key_links:
    - from: package.json
      to: scripts/phase2-no-email-gate.mjs
      via: "npm run phase2:no-email-gate"
      pattern: "phase2:no-email-gate"
---

<objective>
Remove all Resend/transactional-email dead code and env references from the unified app; add an automated grep gate so email paths cannot regress before Phase 8 prune.

Purpose: Scope change (2026-06-14) — ADM-04, CNSG-03 email, CNTC-02 email are OUT; D-24 requires zero email code in `src/`.
Output: Clean `src/`, updated env docs, `npm run phase2:no-email-gate` passes.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-supabase-data-layer-public-flows/02-CONTEXT.md
@.planning/ROADMAP.md

## Scope note (REPLAN)

Code is largely implemented. Public routes at `src/app/api/public/{verify,consign,contact}/route.ts` already have **no** email calls. This plan is cleanup + regression gate only — **no new migrations**, **no Resend**, **no `supabase db push`**.

## Locked decisions

- **D-20/D-21/D-24:** No Resend, no `sendTransactional`, no email env vars in active paths
- **ADM-04:** OUT OF SCOPE for Phase 2
- Root paths: `src/`, `supabase/` (NOT `apps/web/`)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and purge email dead code in src/ and env docs</name>
  <files>src/**, .env.example, README.md, src/lib/services/impl/index.ts</files>
  <action>
    Per D-24, run ripgrep across `src/` for: `resend`, `sendTransactional`, `RESEND_`, `OPERATOR_EMAIL`, `api.resend.com`, `email/send`, `lib/email/`.

    Expected: zero hits in production `src/` (already true — confirm). If any remain (e.g. stale imports, commented blocks), delete them.

    Update `.env.example`:
    - Remove `# OPERATOR_EMAIL=` and any "future notification" copy implying Phase 2 email
    - Keep only Supabase + site URL vars required for public flows

    Update `README.md` env section (if present) to document Supabase vars only — no Resend/OPERATOR_EMAIL.

    Fix stale comment in `src/lib/services/impl/index.ts` line 3 ("localStorage/fixtures") → "Supabase/API adapters for public flows".

    Do NOT touch `supabase/migrations/017_create_email_logs.sql` or `034_*` — email_logs table prune is Phase 8 (DB-02).
  </action>
  <verify>
    <automated>node scripts/phase2-no-email-gate.mjs</automated>
  </verify>
  <done>Zero email-pattern matches in `src/`; `.env.example` has no OPERATOR_EMAIL/RESEND vars; impl/index comment accurate</done>
</task>

<task type="auto">
  <name>Task 2: Add automated no-email grep gate script</name>
  <files>scripts/phase2-no-email-gate.mjs, package.json</files>
  <action>
    Create `scripts/phase2-no-email-gate.mjs` (Node ESM, no dependencies):

    ```javascript
    // Scan src/ only; exit 1 if any pattern matches
    const patterns = [
      /resend/i,
      /sendTransactional/,
      /RESEND_/,
      /OPERATOR_EMAIL/,
      /api\.resend\.com/,
      /lib\/email\//,
    ];
    ```

    - Use `fs.readdirSync` recursive or spawn `npx rg` if available; prefer pure Node for Windows compatibility
    - Ignore `node_modules`, `.next`, `graphify-out`
    - Print file:line for each match; exit 0 if clean

    Add to root `package.json` scripts:
    `"phase2:no-email-gate": "node scripts/phase2-no-email-gate.mjs"`

    Wire into later Plan 04 as part of `phase2:gate` — do not add build dependency here.
  </action>
  <verify>
    <automated>npm run phase2:no-email-gate</automated>
  </verify>
  <done>Script exists, npm script registered, exits 0 on current codebase</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| src/ → external email API | Must not exist in v1 — prevents accidental secret exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Information Disclosure | Resend API key in client bundle | mitigate | Grep gate blocks `RESEND_` in `src/`; keys server-only if ever re-added |
| T-2-02 | Spoofing | Fake operator emails | accept | Email removed from v1; operators use CRM only (D-20) |
</threat_model>

<verification>
```bash
npm run phase2:no-email-gate
rg -i "resend|sendTransactional|RESEND_|OPERATOR_EMAIL" src/ || echo "clean"
```
</verification>

<success_criteria>
- `npm run phase2:no-email-gate` passes
- No Resend/email env vars in `.env.example`
- D-24 satisfied with automated regression gate
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-data-layer-public-flows/02-01-SUMMARY.md`
</output>

---
phase: 02-supabase-data-layer-public-flows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/supabase/migrations/032_add_product_id_marketplace_items.sql
  - apps/web/supabase/migrations/033_storage_consign_submissions.sql
  - apps/web/supabase/migrations/034_email_logs_nullable_user_id.sql
  - apps/web/src/lib/email/sendTransactional.ts
  - packages/shared/src/domain/schemas/verify.ts
  - apps/web/src/lib/schemas/verify.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - ADM-04
must_haves:
  truths:
    - "marketplace_items has product_id column for RLQ-XXXX-XXXX lookups"
    - "consign-submissions storage bucket exists (private, 10MB, image types)"
    - "email_logs.user_id nullable for system-triggered emails"
    - "sendTransactionalEmail helper callable without requireUser"
  artifacts:
    - path: "apps/web/supabase/migrations/032_add_product_id_marketplace_items.sql"
      provides: "product_id column + unique index + GIN on auth.coa_refs"
    - path: "apps/web/supabase/migrations/033_storage_consign_submissions.sql"
      provides: "consign-submissions bucket"
    - path: "apps/web/src/lib/email/sendTransactional.ts"
      provides: "Shared Resend helper for public routes"
  key_links:
    - from: "apps/web/src/lib/email/sendTransactional.ts"
      to: "https://api.resend.com/emails"
      via: "fetch with RESEND_API_KEY"
      pattern: "api.resend.com/emails"
---

<objective>
Tạo foundation migrations và shared email helper — blocker cho verify/consign/contact public routes.

Purpose: Phase 2 cần `product_id` column, `consign-submissions` bucket, và nullable `email_logs.user_id` trước khi implement flows (02-RESEARCH.md).
Output: 3 SQL migrations + `sendTransactional.ts` + extended `VerifyResult` schema fields.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-supabase-data-layer-public-flows/02-CONTEXT.md
@.planning/phases/02-supabase-data-layer-public-flows/02-RESEARCH.md
@apps/web/supabase/migrations/002_create_marketplace_items.sql
@apps/web/supabase/migrations/011_storage_crm.sql
@apps/web/supabase/migrations/017_create_email_logs.sql
@apps/web/src/app/api/email/send/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migrations (product_id, storage bucket, email_logs)</name>
  <read_first>
    - apps/web/supabase/migrations/002_create_marketplace_items.sql
    - apps/web/supabase/migrations/011_storage_crm.sql
    - apps/web/supabase/migrations/017_create_email_logs.sql
    - .planning/phases/02-supabase-data-layer-public-flows/02-RESEARCH.md (migrations section)
  </read_first>
  <files>
    apps/web/supabase/migrations/032_add_product_id_marketplace_items.sql
    apps/web/supabase/migrations/033_storage_consign_submissions.sql
    apps/web/supabase/migrations/034_email_logs_nullable_user_id.sql
  </files>
  <action>
    Create three migrations following existing numbering:

    **032_add_product_id_marketplace_items.sql:**
    - `alter table public.marketplace_items add column product_id text;`
    - `create unique index marketplace_items_product_id_idx on public.marketplace_items(product_id) where product_id is not null;`
    - `create index marketplace_items_auth_coa_refs_idx on public.marketplace_items using gin ((auth->'coa_refs'));`
    - Comment: product_id stores RLQ-XXXX-XXXX format; COA refs remain in auth.coa_refs JSONB

    **033_storage_consign_submissions.sql:**
    - Mirror pattern from 011_storage_crm.sql
    - Bucket id: `consign-submissions`, public: false, file_size_limit: 10485760
    - allowed_mime_types: image/jpeg, image/png, image/webp
    - No anon upload policies — service role bypasses RLS (D-15)

    **034_email_logs_nullable_user_id.sql:**
    - `alter table public.email_logs alter column user_id drop not null;`
    - Comment: allows system-triggered emails from public routes without authenticated user
  </action>
  <acceptance_criteria>
    - `test -f apps/web/supabase/migrations/032_add_product_id_marketplace_items.sql` exits 0
    - `test -f apps/web/supabase/migrations/033_storage_consign_submissions.sql` exits 0
    - `test -f apps/web/supabase/migrations/034_email_logs_nullable_user_id.sql` exits 0
    - `grep -q "product_id" apps/web/supabase/migrations/032_add_product_id_marketplace_items.sql` exits 0
    - `grep -q "consign-submissions" apps/web/supabase/migrations/033_storage_consign_submissions.sql` exits 0
    - `grep -q "drop not null" apps/web/supabase/migrations/034_email_logs_nullable_user_id.sql` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -l product_id apps/web/supabase/migrations/032*.sql</automated>
  </verify>
  <done>Three migration files exist with correct schema changes</done>
</task>

<task type="auto">
  <name>Task 2: Extend VerifyResult schema</name>
  <read_first>
    - packages/shared/src/domain/schemas/verify.ts
    - apps/web/src/lib/schemas/verify.ts
    - .planning/phases/02-supabase-data-layer-public-flows/02-CONTEXT.md (VRFY-03, VRFY-04)
  </read_first>
  <files>
    packages/shared/src/domain/schemas/verify.ts
    apps/web/src/lib/schemas/verify.ts
  </files>
  <action>
    Extend VerifyResultSchema with optional fields for VRFY-03/04 (backward compatible — all optional):

    ```typescript
    marketplaceSlug: z.string().optional(),
    marketplaceUrl: z.string().optional(),
    signerNames: z.array(z.string()).optional(),
    itemType: z.string().optional(),
    grade: z.string().optional(),
    heroImageUrl: z.string().optional(),
    galleryImageUrls: z.array(z.string()).optional(),
    ```

    Keep existing required fields unchanged. Mirror changes in apps/web/src/lib/schemas/verify.ts if it duplicates shared schema (or re-export from shared if already aligned).

    Do NOT change VerifyStatus enum values (qualified/inconclusive/disqualified).
  </action>
  <acceptance_criteria>
    - `grep -q "marketplaceSlug" packages/shared/src/domain/schemas/verify.ts` exits 0
    - `grep -q "signerNames" packages/shared/src/domain/schemas/verify.ts` exits 0
    - `pnpm --filter @relique/shared check-types 2>/dev/null || pnpm --filter web check-types` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web check-types</automated>
  </verify>
  <done>VerifyResult schema extended for certificate details + marketplace link</done>
</task>

<task type="auto">
  <name>Task 3: Create sendTransactional email helper</name>
  <read_first>
    - apps/web/src/app/api/email/send/route.ts
    - apps/web/supabase/migrations/017_create_email_logs.sql
    - .planning/phases/02-supabase-data-layer-public-flows/02-CONTEXT.md (D-20, D-21)
  </read_first>
  <files>
    apps/web/src/lib/email/sendTransactional.ts
  </files>
  <action>
    Create `apps/web/src/lib/email/sendTransactional.ts`:

    Export async function `sendTransactionalEmail(params)`:
    - Params: `{ to, subject, body, entityType?, entityId?, userId? }`
    - Read `RESEND_API_KEY`, `RESEND_FROM_EMAIL` (default support@relique.co)
    - Call Resend API via fetch (same pattern as /api/email/send)
    - Log to `email_logs` via service role — `user_id` nullable (use params.userId or null)
    - Return `{ ok: true, id }` or `{ ok: false, error }` — do NOT throw on missing API key; return error object
    - Export `getOperatorEmail()` reading `process.env.OPERATOR_EMAIL || process.env.RESEND_OPERATOR_TO`

    Do NOT require auth. Server-only module.
  </action>
  <acceptance_criteria>
    - `test -f apps/web/src/lib/email/sendTransactional.ts` exits 0
    - `grep -q "api.resend.com/emails" apps/web/src/lib/email/sendTransactional.ts` exits 0
    - `grep -q "getOperatorEmail" apps/web/src/lib/email/sendTransactional.ts` exits 0
    - `grep -q "email_logs" apps/web/src/lib/email/sendTransactional.ts` exits 0
    - `pnpm --filter web check-types` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web check-types</automated>
  </verify>
  <done>Transactional email helper ready for public routes</done>
</task>

<task type="checkpoint" autonomous="false">
  <name>[BLOCKING] Apply Supabase migrations</name>
  <read_first>
    - apps/web/supabase/MIGRATIONS.md
    - apps/web/supabase/migrations/032_add_product_id_marketplace_items.sql
    - apps/web/supabase/migrations/033_storage_consign_submissions.sql
    - apps/web/supabase/migrations/034_email_logs_nullable_user_id.sql
  </read_first>
  <action>
    Apply migrations 032–034 to linked Supabase project:

    ```bash
    cd apps/web && supabase db push
    ```

    If non-TTY or requires token: set `SUPABASE_ACCESS_TOKEN` and run push.

    Verify in Supabase dashboard:
    - `marketplace_items.product_id` column exists
    - `consign-submissions` bucket exists
    - `email_logs.user_id` is nullable

    If push fails due to missing Supabase link, document in SUMMARY and flag human_needed — do NOT skip silently.
  </action>
  <acceptance_criteria>
    - Migration files 032–034 committed
    - `supabase db push` exits 0 OR SUMMARY documents blocker with human_needed flag
    - Dashboard shows product_id column OR human documents manual apply steps
  </acceptance_criteria>
  <verify>
    <manual>Supabase dashboard: marketplace_items.product_id column visible</manual>
  </verify>
  <done>Migrations applied to remote/local Supabase or blocker documented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Resend API | API key server-only |
| Server → Supabase | Service role key server-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Info Disclosure | sendTransactional.ts | mitigate | RESEND_API_KEY never imported in client components |
| T-2-02 | Tampering | Migration 032 | accept | product_id unique index prevents duplicate RLQ IDs |
</threat_model>

<verification>
- `pnpm --filter web check-types` passes
- Migration files 032, 033, 034 exist
- sendTransactional.ts exports getOperatorEmail + sendTransactionalEmail
</verification>

<success_criteria>
- Foundation schema ready for verify/consign/contact routes
- Email helper shared across public flows
- VerifyResult schema extended for VRFY-03/04
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-data-layer-public-flows/02-01-SUMMARY.md`
</output>

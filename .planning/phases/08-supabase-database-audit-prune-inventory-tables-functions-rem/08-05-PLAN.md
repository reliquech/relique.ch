---
phase: 08-supabase-database-audit-prune-inventory-tables-functions-rem
plan: 05
type: execute
wave: 4
depends_on:
  - "08-02"
  - "08-04"
files_modified:
  - src/lib/supabase/types.ts
  - package.json
  - scripts/smoke-supabase.mjs
  - supabase/MIGRATIONS.md
  - .planning/REQUIREMENTS.md
  - .planning/codebase/INTEGRATIONS.md
autonomous: false
requirements:
  - DB-06
  - DB-07
  - DB-08
user_setup:
  - service: supabase
    why: "Regenerate TypeScript types from live schema post-036"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard → Project Settings → API"
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (for CLI gen types)"
must_haves:
  truths:
    - "types.ts has no email_logs table definition after regen"
    - "npm run check-types and npm run build pass"
    - "Smoke script confirms health + marketplace + dashboard RPC path reachable"
    - "REQUIREMENTS.md defines DB-01 through DB-08"
  artifacts:
    - path: src/lib/supabase/types.ts
      provides: "Regenerated Database types"
      contains: "Database"
    - path: scripts/smoke-supabase.mjs
      provides: "Post-prune smoke checks"
      exports: ["runSmoke"]
    - path: package.json
      provides: "gen:types script"
      contains: "gen:types"
  key_links:
    - from: scripts/smoke-supabase.mjs
      to: /api/health
      via: "fetch health check"
      pattern: "api/health"
    - from: src/lib/supabase/types.ts
      to: src/app/api/dashboard/route.ts
      via: "RPC type names"
      pattern: "crm_dashboard_summary"
---

<objective>
Regenerate Supabase types post-prune, verify build + smoke paths, finalize Phase 8 documentation including DB-01–DB-08 requirement definitions.

Purpose: App types match pruned schema; operators have runnable verification; planning artifacts complete.
Output: Updated types, smoke script, docs, build gate pass
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-supabase-database-audit-prune-inventory-tables-functions-rem/08-02-SUMMARY.md
@.planning/phases/08-supabase-database-audit-prune-inventory-tables-functions-rem/08-04-SUMMARY.md
@src/lib/supabase/types.ts
@package.json
@supabase/MIGRATIONS.md

**Prerequisite:** Migration 036 applied on remote (08-02 checkpoint complete).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate Supabase types and add gen script</name>
  <files>package.json, src/lib/supabase/types.ts</files>
  <action>
1. Add npm script to `package.json`:
```json
"gen:types": "supabase gen types typescript --linked > src/lib/supabase/types.ts"
```
If project uses `--project-id` instead of linked, document env `SUPABASE_PROJECT_ID` in script comment.

2. Regenerate types (requires 036 applied):
```bash
npx supabase gen types typescript --linked > src/lib/supabase/types.ts
```
Fallback if not linked:
```bash
npx supabase gen types typescript --project-id $SUPABASE_PROJECT_ID > src/lib/supabase/types.ts
```

3. Verify `email_logs` absent from generated types
4. Verify `admin_upsert_profile` absent from Functions section
5. Grep `src/` for `email_logs` — remove any stale imports/references (should be none per 08-01)

6. Run `npm run check-types` — fix any breakages from removed table (unlikely if no app usage)

Do NOT manually edit types except formatting header comment:
```typescript
// Generated by `npm run gen:types` — regenerate after schema migrations
```
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <done>types.ts regenerated without email_logs; check-types passes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Smoke script, build gate, and Phase 8 docs</name>
  <files>scripts/smoke-supabase.mjs, .planning/REQUIREMENTS.md, .planning/codebase/INTEGRATIONS.md, supabase/MIGRATIONS.md</files>
  <behavior>
    - Health: GET /api/health returns 200 with supabase connectivity indicator
    - Marketplace: GET /api/marketplace returns 200 JSON (items array, may be empty)
    - Verify route exists: GET /api/public/verify?code=SMOKE_TEST returns 200 or 404 (not 500)
    - Types static check: types.ts must not contain "email_logs"
    - Dashboard RPCs: if SMOKE_AUTH_COOKIE env set, GET /api/dashboard?range=30 returns 200; else skip with warn
  </behavior>
  <action>
**Smoke script** — Create `scripts/smoke-supabase.mjs`:
- Reads `NEXT_PUBLIC_SITE_URL` (default `http://localhost:1300`)
- Runs fetch checks above with timeout 10s each; exits 0 on pass
- Static check: types.ts must not contain `email_logs`
- Add npm script: `"smoke:supabase": "node scripts/smoke-supabase.mjs"`
- Run `npm run build` after types regen

**Docs (DB-08)** — Update `.planning/REQUIREMENTS.md` with DB-01–DB-08 definitions and Phase 8 coverage rows.

Update `.planning/codebase/INTEGRATIONS.md`: remove `email_logs` from key tables; remove email audit bullet.

Update `supabase/MIGRATIONS.md`: add `npm run gen:types`, `npm run smoke:supabase`; link SUPABASE_USAGE.md, RLS_AUDIT.md, INDEX_AUDIT.md; 036 rollback note.
  </action>
  <verify>
    <automated>node -e "const req=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8');['DB-01','DB-08'].forEach(id=>{if(!req.includes(id))throw new Error('missing '+id)});" && node scripts/smoke-supabase.mjs && npm run build</automated>
  </verify>
  <done>Smoke script + docs complete; build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify smoke and dashboard RPCs</name>
  <files>scripts/smoke-supabase.mjs</files>
  <action>Pause for human to run full verification after automated smoke + build from Task 2. Confirm dashboard charts load when authenticated.</action>
  <what-built>Types regen, smoke script, build gate, Phase 8 docs</what-built>
  <how-to-verify>
1. Confirm 036 was applied (from 08-02)
2. Run `npm run gen:types` (if types not already fresh)
3. Run `npm run check-types && npm run build`
4. Start dev: `npm run dev`
5. Run `npm run smoke:supabase` — all checks pass (or dashboard skipped without auth)
6. Optional manual: login admin → dashboard loads charts (7 RPCs)
7. Optional: `GET /api/public/verify?code=...` with real cert code
8. Fresh project test (optional): new Supabase project → apply `000_baseline.sql` only → smoke pass
  </how-to-verify>
  <verify>
    <automated>MISSING — human checkpoint; resume after user types "approved"</automated>
  </verify>
  <resume-signal>Type "approved" if smoke + build pass, or describe failures</resume-signal>
  <done>Human confirms smoke + build pass; optional dashboard RPC visual check</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gen types → service_role schema | Types expose table shapes to codebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | I | types.ts | accept | Generated from schema; no secrets in types file |
| T-08-12 | D | smoke script | mitigate | Read-only GET requests; no destructive ops |
</threat_model>

<verification>
- `npm run check-types` passes
- `npm run build` passes
- `node scripts/smoke-supabase.mjs` passes
- REQUIREMENTS.md has DB-01 through DB-08
</verification>

<success_criteria>
DB-06, DB-07, DB-08 satisfied: types match pruned schema, smoke + build pass, requirements and integration docs complete.
</success_criteria>

<output>
After completion, create `.planning/phases/08-supabase-database-audit-prune-inventory-tables-functions-rem/08-05-SUMMARY.md`
</output>

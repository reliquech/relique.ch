---
phase: 08-supabase-database-audit-prune-inventory-tables-functions-rem
plan: 04
type: execute
wave: 3
depends_on:
  - "08-02"
  - "08-03"
files_modified:
  - supabase/migrations/000_baseline.sql
  - supabase/migrations/legacy/README.md
  - supabase/MIGRATIONS.md
  - supabase/MIGRATION_MANIFEST.md
autonomous: true
requirements:
  - DB-03
  - DB-08
user_setup: []
must_haves:
  truths:
    - "Fresh Supabase project can bootstrap with single 000_baseline.sql file"
    - "Brownfield projects continue 001-036 incremental chain unchanged"
    - "Baseline excludes email_logs and admin_upsert_profile (post-036 state)"
  artifacts:
    - path: supabase/migrations/000_baseline.sql
      provides: "Squashed schema for fresh installs"
      min_lines: 200
    - path: supabase/migrations/legacy/README.md
      provides: "Archive explanation for 001-035"
      contains: "brownfield"
    - path: supabase/MIGRATIONS.md
      provides: "Dual-path apply documentation"
      contains: "000_baseline"
  key_links:
    - from: supabase/MIGRATIONS.md
      to: supabase/migrations/000_baseline.sql
      via: "fresh install path"
      pattern: "000_baseline"
    - from: supabase/migrations/000_baseline.sql
      to: supabase/migrations/legacy/
      via: "001-035 archived, not deleted"
      pattern: "legacy"
---

<objective>
Deliver `000_baseline.sql` for fresh Supabase installs — final schema state (post-036 prune) in one file; archive incremental 001-035 to `legacy/` without breaking brownfield apply path.

Purpose: New envs bootstrap in one step; production DBs keep incremental chain (no squash on live DB — locked D-08-04).
Output: `000_baseline.sql`, `migrations/legacy/`, updated MIGRATIONS.md + manifest
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-supabase-database-audit-prune-inventory-tables-functions-rem/08-02-SUMMARY.md
@supabase/RLS_AUDIT.md
@supabase/INDEX_AUDIT.md
@supabase/SUPABASE_USAGE.md
@supabase/MIGRATION_MANIFEST.md

## Locked decision D-08-04

**No squash on live DB** — `000_baseline.sql` is for **fresh installs only**. Never replace 001-036 on applied production projects.

## Baseline content rules

Include consolidated final state from chain 001-035 **minus**:
- `email_logs` table (017, 034) — already pruned in baseline target state
- `admin_upsert_profile` function — drop from baseline

Consolidate duplicates:
- Single `handle_updated_at()` + `handle_new_user()` definition
- RLS policies: merge 005+009 effective policies per RLS_AUDIT.md
- 7 CRM RPCs: use final bodies from 025 (includes 014+020 extensions)
- All 035 indexes included
- 3 storage buckets with policies
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 000_baseline.sql squashed schema</name>
  <files>supabase/migrations/000_baseline.sql</files>
  <action>
Create `supabase/migrations/000_baseline.sql` — single ordered DDL file representing **final** schema:

**Structure (use section comments):**
1. Extensions (`uuid-ossp`, `pgcrypto` if used)
2. Tables (profiles → marketplace → consign → audit → CRM core → extensions) — **no email_logs**
3. Indexes (006 + per-table + 032 + 035 composites)
4. Functions: triggers + 7 CRM RPCs (final versions) — **no admin_upsert_profile**
5. Triggers
6. RLS enable + policies (consolidated from 002-031, 005, 009 — per RLS_AUDIT.md)
7. Storage buckets (008, 011, 033)
8. Seed data (pipeline_stages from 012)

**Method:** Read each migration 001-035 in order; merge into baseline, resolving `create or replace` to final form. Skip 017/034 email_logs entirely. From 030, keep trigger/function helpers only — omit `admin_upsert_profile`.

Add file header:
```sql
-- Relique.co baseline schema (squashed 001-035, post-036 prune state)
-- FRESH INSTALLS ONLY — run this single file OR use legacy incremental chain
-- Generated: Phase 8 — do not apply on brownfield DBs with 001+ already applied
```

Validate: `grep -c "email_logs" 000_baseline.sql` must be 0; `grep "admin_upsert_profile"` must be 0; `grep "crm_dashboard_summary"` must be 1+.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/000_baseline.sql','utf8');if(/email_logs/i.test(s))throw new Error('baseline must not include email_logs');if(/admin_upsert_profile/i.test(s))throw new Error('baseline must not include dead rpc');if(!/crm_dashboard_summary/i.test(s))throw new Error('missing dashboard rpc');if(s.split('\n').length<200)throw new Error('baseline too short');"</automated>
  </verify>
  <done>000_baseline.sql exists; post-prune schema; 7 RPCs + 3 buckets + 035 indexes present</done>
</task>

<task type="auto">
  <name>Task 2: Archive legacy chain and document dual-path</name>
  <files>supabase/migrations/legacy/README.md, supabase/MIGRATIONS.md, supabase/MIGRATION_MANIFEST.md</files>
  <action>
1. Create `supabase/migrations/legacy/` directory
2. **Move** (not delete) files `001_*.sql` through `035_*.sql` into `legacy/` preserving filenames
3. Keep active migration folder containing:
   - `000_baseline.sql` (fresh path)
   - `036_prune_dead_schema.sql` (brownfield only — fresh baseline already excludes pruned objects)

4. Write `supabase/migrations/legacy/README.md`:
   - Explains brownfield projects use legacy/001-036 chain
   - Fresh projects use `000_baseline.sql` only
   - Warning: never apply both paths on same DB

5. Update `supabase/MIGRATIONS.md` with **Dual-path strategies** table:

| Scenario | Files to apply |
|----------|----------------|
| **Fresh project** | `000_baseline.sql` only |
| **Existing brownfield** | `legacy/001` → `legacy/035`, then `036_prune_dead_schema.sql` |
| **Already on 001-035** | `036` only |

6. Update `MIGRATION_MANIFEST.md`:
   - Baseline section: `000_baseline.sql` purpose
   - Legacy archive pointer
   - Note: `supabase db push` on linked project applies only non-legacy files by default — document that brownfield must apply legacy chain manually or via migration history table

**Supabase CLI note:** If `db push` skips legacy/, document manual apply order for brownfield. Fresh installs: SQL Editor paste `000_baseline.sql`.

Do NOT run db push in this plan.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const path=require('path');if(!fs.existsSync('supabase/migrations/legacy/README.md'))throw new Error('missing legacy readme');const legacy=fs.readdirSync('supabase/migrations/legacy').filter(f=>f.endsWith('.sql'));if(legacy.length<35)throw new Error('expected 35 legacy files, got '+legacy.length);const active=fs.readdirSync('supabase/migrations').filter(f=>f.endsWith('.sql'));if(!active.includes('000_baseline.sql'))throw new Error('missing baseline');const mig=fs.readFileSync('supabase/MIGRATIONS.md','utf8');if(!mig.includes('000_baseline'))throw new Error('MIGRATIONS.md missing dual-path');"</automated>
  </verify>
  <done>001-035 archived; dual-path docs complete; active folder has 000 + 036 only</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| baseline → fresh DB | Full schema DDL — must not run on populated brownfield DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | D | 000_baseline on brownfield | mitigate | README + MIGRATIONS.md warnings; separate legacy path |
| T-08-10 | T | squashed RLS | mitigate | Cross-check policies against RLS_AUDIT.md before commit |
</threat_model>

<verification>
- 000_baseline.sql excludes pruned objects
- 35 files in legacy/
- MIGRATIONS.md documents fresh vs brownfield paths
</verification>

<success_criteria>
DB-03 satisfied: fresh-install baseline exists; DB-08 partial: migration docs updated with dual-path strategy.
</success_criteria>

<output>
After completion, create `.planning/phases/08-supabase-database-audit-prune-inventory-tables-functions-rem/08-04-SUMMARY.md`
</output>

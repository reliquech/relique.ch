---
phase: 08-supabase-database-audit-prune-inventory-tables-functions-rem
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - supabase/migrations/036_prune_dead_schema.sql
  - supabase/MIGRATION_MANIFEST.md
autonomous: false
requirements:
  - DB-02
user_setup:
  - service: supabase
    why: "Apply prune migration to remote/hosted DB"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard → Project Settings → API"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API (service_role)"
    dashboard_config:
      - task: "Alternative: paste 036 SQL in SQL Editor if CLI unavailable"
        location: "Supabase Dashboard → SQL Editor"
must_haves:
  truths:
    - "email_logs table no longer exists after migration 036 applied"
    - "admin_upsert_profile function no longer exists after migration 036 applied"
    - "Incremental chain 001-035 unchanged — prune is additive only"
  artifacts:
    - path: supabase/migrations/036_prune_dead_schema.sql
      provides: "DROP dead schema objects"
      contains: "drop table"
    - path: supabase/MIGRATION_MANIFEST.md
      provides: "036 documented in manifest"
      contains: "036"
  key_links:
    - from: supabase/migrations/036_prune_dead_schema.sql
      to: public.email_logs
      via: "DROP TABLE CASCADE"
      pattern: "drop table.*email_logs"
    - from: supabase/migrations/036_prune_dead_schema.sql
      to: public.admin_upsert_profile
      via: "DROP FUNCTION"
      pattern: "drop function.*admin_upsert_profile"
---

<objective>
Prune confirmed dead schema via additive migration 036 — drop `email_logs` and `admin_upsert_profile` without rewriting history migrations 001-035.

Purpose: Schema matches app reality after Resend removal; eliminate unused RPC surface.
Output: `036_prune_dead_schema.sql` + updated manifest + [BLOCKING] remote apply
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-supabase-database-audit-prune-inventory-tables-functions-rem/08-01-SUMMARY.md
@supabase/SUPABASE_USAGE.md
@supabase/migrations/017_create_email_logs.sql
@supabase/migrations/030_admin_create_account_function.sql
@supabase/migrations/034_email_logs_nullable_user_id.sql

## Locked decisions

- **D-08-01:** DROP `email_logs` — Resend removed from v1
- **D-08-02:** DROP `admin_upsert_profile` — keep trigger helpers from 030 (`handle_updated_at`, `handle_new_user`) intact
- **D-08-03:** Do NOT squash/modify 001-035 on live DB
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 036 prune DDL</name>
  <files>supabase/migrations/036_prune_dead_schema.sql</files>
  <action>
Create `supabase/migrations/036_prune_dead_schema.sql` with idempotent-safe drops:

```sql
-- Phase 8: prune dead schema (Resend removed, unused RPC)
-- Safe on DBs that never had email_logs (IF EXISTS guards)

-- 1. email_logs (migrations 017, 034)
drop policy if exists "Users can read own email logs" on public.email_logs;
drop policy if exists "Users can insert own email logs" on public.email_logs;
drop table if exists public.email_logs cascade;

-- 2. admin_upsert_profile RPC (migration 030) — NOT the trigger functions
drop function if exists public.admin_upsert_profile(uuid, text, text, text);
```

**Critical — do NOT drop:**
- `handle_updated_at()`, `handle_new_user()` — still used by triggers
- Any of the 7 dashboard CRM RPCs
- Storage buckets or bucket policies

Grep `src/` before commit — confirm zero `email_logs` and `admin_upsert_profile` references. If any found in `src/lib/supabase/types.ts`, note for 08-05 regen (do not edit types here).

Add header comment referencing SUPABASE_USAGE.md prune queue.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/036_prune_dead_schema.sql','utf8');if(!/drop table if exists public\.email_logs/i.test(s))throw new Error('missing email_logs drop');if(!/drop function if exists public\.admin_upsert_profile/i.test(s))throw new Error('missing rpc drop');if(/drop function.*handle_new_user/i.test(s))throw new Error('must not drop triggers');"</automated>
  </verify>
  <done>036 file exists with safe IF EXISTS drops; trigger functions preserved</done>
</task>

<task type="auto">
  <name>Task 2: Update migration manifest for 036</name>
  <files>supabase/MIGRATION_MANIFEST.md</files>
  <action>
Add section **## Prune (036)** to `supabase/MIGRATION_MANIFEST.md`:

| File | Purpose |
|------|---------|
| 036 | Drop `email_logs` + `admin_upsert_profile` (Phase 8 prune) |

Update overlap notes:
- 017 + 034 superseded by 036 on applied DBs (table removed)
- 030 `admin_upsert_profile` removed; trigger/function helpers from 030 remain

Update file count note: brownfield chain = 001-036 (36 files after prune migration added).

Document apply order: existing envs apply **035 first** (if pending), then **036**.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('supabase/MIGRATION_MANIFEST.md','utf8');if(!t.includes('036'))throw new Error('manifest missing 036');"</automated>
  </verify>
  <done>Manifest documents 036 purpose, apply order, and brownfield chain length</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>[BLOCKING] Apply migration 036 to remote Supabase</name>
  <files>supabase/migrations/036_prune_dead_schema.sql</files>
  <action>
Executor attempts automated apply first:

```bash
# From repo root — requires linked project or SUPABASE_ACCESS_TOKEN
supabase db push
```

If CLI fails (no link/auth), pause and instruct user to apply manually.

**Manual fallback:** Supabase Dashboard → SQL Editor → paste contents of `036_prune_dead_schema.sql` → Run.
  </action>
  <how-to-verify>
1. Confirm 035 already applied on target project (indexes from Phase 7)
2. Run `supabase db push` OR paste 036 SQL in Dashboard
3. Verify in Dashboard → Table Editor: `email_logs` absent
4. Verify in Dashboard → Database → Functions: `admin_upsert_profile` absent
5. Verify `handle_new_user` and `handle_updated_at` still exist
  </how-to-verify>
  <verify>
    <automated>MISSING — requires remote DB; human confirms apply then executor runs: node -e "console.log('Resume after user confirms 036 applied')"</automated>
  </verify>
  <resume-signal>Type "036 applied" when remote DB push/SQL complete, or describe blocker</resume-signal>
  <done>Remote DB has 036 applied; email_logs gone; admin_upsert_profile gone; triggers intact</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → production DB | DDL drops are irreversible without restore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | D | 036 DROP TABLE | mitigate | IF EXISTS guards; only confirmed-dead objects per SUPABASE_USAGE.md |
| T-08-04 | E | admin_upsert_profile drop | accept | Function unused; users API uses direct upsert |
| T-08-05 | I | email_logs drop | accept | No app queries; Resend audit trail out of v1 scope |
</threat_model>

<verification>
- 036 SQL reviewed: drops only email_logs + admin_upsert_profile
- [BLOCKING] Remote apply confirmed by user
- 001-035 files untouched
</verification>

<success_criteria>
DB-02 satisfied: dead schema pruned via migration 036 on remote DB; incremental history preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/08-supabase-database-audit-prune-inventory-tables-functions-rem/08-02-SUMMARY.md`
</output>

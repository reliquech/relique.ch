---
phase: 08-supabase-database-audit-prune-inventory-tables-functions-rem
plan: 03
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - supabase/RLS_AUDIT.md
  - supabase/INDEX_AUDIT.md
autonomous: true
requirements:
  - DB-04
  - DB-05
user_setup: []
must_haves:
  truths:
    - "Every public-facing table has documented RLS policy map aligned with SEC-04"
    - "Hot-path queries have index coverage documented with migration file refs"
    - "Service-role bypass pattern documented for admin-only tables"
  artifacts:
    - path: supabase/RLS_AUDIT.md
      provides: "Table × role × operation matrix"
      contains: "marketplace_items"
    - path: supabase/INDEX_AUDIT.md
      provides: "Index coverage for browse/CRM/audit paths"
      contains: "marketplace_items_public_browse_idx"
  key_links:
    - from: supabase/RLS_AUDIT.md
      to: supabase/migrations/002_create_marketplace_items.sql
      via: "published+public SELECT policy"
      pattern: "state_lifecycle.*published"
    - from: supabase/INDEX_AUDIT.md
      to: supabase/migrations/035_optimize_public_browse_indexes.sql
      via: "035 composite indexes"
      pattern: "marketplace_items_public_browse_idx"
---

<objective>
Audit RLS policies and indexes against actual app access patterns — document matrix for operators, align public read paths with SEC-04, confirm Phase 7 indexes cover hot paths.

Purpose: Post-prune schema must have defensible security and performance posture without rewriting applied migrations.
Output: `supabase/RLS_AUDIT.md` + `supabase/INDEX_AUDIT.md`
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-supabase-database-audit-prune-inventory-tables-functions-rem/08-CONTEXT.md
@supabase/SUPABASE_USAGE.md
@supabase/migrations/005_create_rls_policies.sql
@supabase/migrations/009_fix_rls_performance.sql
@supabase/migrations/035_optimize_public_browse_indexes.sql
@src/app/api/marketplace/route.ts
@src/lib/supabase/server.ts

## SEC-04 alignment target

Public marketplace browse must only expose `state_lifecycle = 'published'` AND `state_visibility = 'public'`. Verify route uses `createAnonClient()` for unauthenticated reads.
</context>

<tasks>

<task type="auto">
  <name>Task 1: RLS audit matrix</name>
  <files>supabase/RLS_AUDIT.md</files>
  <action>
Create `supabase/RLS_AUDIT.md` with:

### 1. Summary table (all `public` tables from SUPABASE_USAGE.md KEEP list)

Columns: Table | RLS enabled | anon SELECT | anon INSERT/UPDATE/DELETE | authenticated policies | service_role pattern | SEC-04 notes

### 2. Per-table policy detail

For each table, list policies from migrations (grep `create policy` across `supabase/migrations/*.sql`). Note 005 vs 009 overlap — document **effective** policy after both applied.

**Critical SEC-04 checks:**
- `marketplace_items`: anon can SELECT only published+public (policy in 002; confirm no broader leak)
- `consigned_items`, `audit_logs`, `leads`, CRM tables: **no anon access** — admin via service_role only
- `profiles`: authenticated read own profile
- Post-036: `email_logs` section marked **REMOVED**

### 3. Client usage map

| Access pattern | Client | Routes |
|----------------|--------|--------|
| Public browse | `createAnonClient()` | `/api/marketplace`, `/api/public/verify` |
| Admin mutate | `createServiceRoleClient()` + requireRole | `/api/leads`, `/api/consigned`, etc. |

### 4. Findings + recommendations

List any policy gaps or redundant policies. **Do NOT create new migration files** in this plan — recommendations only (e.g. "future migration could merge 005+009").

Reference Phase 3 SEC-04 requirement ID in notes.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('supabase/RLS_AUDIT.md','utf8');['marketplace_items','SEC-04','service_role','email_logs'].forEach(s=>{if(!t.includes(s))throw new Error('missing '+s)});"</automated>
  </verify>
  <done>RLS_AUDIT.md covers all KEEP tables; SEC-04 marketplace rule documented; service-role pattern clear</done>
</task>

<task type="auto">
  <name>Task 2: Index audit for hot paths</name>
  <files>supabase/INDEX_AUDIT.md</files>
  <action>
Create `supabase/INDEX_AUDIT.md` auditing indexes from migrations 006 + 032 + 035 and per-table indexes in CRM migrations.

### Hot paths to analyze (grep query patterns in `src/app/api/`):

1. **Public marketplace browse** — `marketplace_items` filter `state_lifecycle`, `state_visibility`, sort `price_amount`
2. **Verify lookup** — `product_id` / cert code (032 indexes)
3. **CRM leads queue** — `leads` by `status`, `source`, `created_at`
4. **Consign queue** — `consigned_items` by `status`, `created_at`
5. **Dashboard RPCs** — note "RPC-internal queries; indexes on underlying tables"
6. **audit_logs** — high write volume; document existing indexes + retention deferral
7. **alert_rules run** — `alert-rules/run/route.ts` query patterns

### Format per path:

- Query source (file:line)
- WHERE/ORDER BY columns
- Matching index (migration file) or **GAP**
- Verdict: COVERED | GAP | DEFER

### 035 confirmation

Explicitly confirm these exist post-035:
- `marketplace_items_public_browse_idx`
- `leads_source_status_idx`
- `consigned_items_queue_idx`

If GAP found that blocks production (unlikely after 035), document as recommendation only — no new migration unless critical (would be 037, out of scope unless user approves).

Cross-link to `RLS_AUDIT.md` and `MIGRATION_MANIFEST.md`.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('supabase/INDEX_AUDIT.md','utf8');['marketplace_items_public_browse_idx','leads_source_status_idx','consigned_items_queue_idx','product_id'].forEach(s=>{if(!t.includes(s))throw new Error('missing '+s)});"</automated>
  </verify>
  <done>INDEX_AUDIT.md documents hot paths; 035 indexes confirmed COVERED; gaps listed with DEFER/recommendation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon client → PostgreSQL | RLS enforces public read scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | I | marketplace_items RLS | mitigate | Document SEC-04 policy; verify anon SELECT scoped to published+public |
| T-08-07 | E | service_role admin routes | mitigate | Document requireUser/requireRole on all mutate routes per RLS_AUDIT client map |
| T-08-08 | D | audit_logs write volume | accept | Document retention deferral; no partition in v1 |
</threat_model>

<verification>
- RLS_AUDIT.md complete for KEEP tables
- INDEX_AUDIT.md confirms 035 coverage on three composite indexes
- No migration files created (audit-only)
</verification>

<success_criteria>
DB-04 and DB-05 satisfied: RLS matrix and index audit docs exist with SEC-04 alignment and hot-path coverage analysis.
</success_criteria>

<output>
After completion, create `.planning/phases/08-supabase-database-audit-prune-inventory-tables-functions-rem/08-03-SUMMARY.md`
</output>

---
phase: 07-supabase-migrations-optimize
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/MIGRATIONS.md
  - supabase/MIGRATION_MANIFEST.md
  - supabase/STORAGE_GUIDE.md
  - README.md
autonomous: true
requirements:
  - MIG-01
  - MIG-02
  - MIG-03
  - MIG-04
  - MIG-06
  - MIG-07
  - MIG-08
must_haves:
  truths:
    - "Developer finds canonical migration path at supabase/migrations/ (root) in docs — no apps/web or apps/admin paths"
    - "MIGRATION_MANIFEST lists every migration 001–035 with purpose, overlaps, buckets, and RPCs"
    - "Fresh baseline squash is documented as deferred; brownfield chain 001–035 is preserved"
    - "Apply path (CLI db push + SQL Editor) is documented for existing and fresh envs"
  artifacts:
    - path: supabase/MIGRATIONS.md
      provides: "Root migration apply guide"
      contains: "supabase/migrations/"
    - path: supabase/MIGRATION_MANIFEST.md
      provides: "Per-file inventory + overlap + buckets + RPC tables"
      contains: "035"
    - path: supabase/STORAGE_GUIDE.md
      provides: "Storage setup without stale monorepo paths"
      contains: "supabase/migrations/"
  key_links:
    - from: README.md
      to: supabase/MIGRATIONS.md
      via: "Supabase / Database section link"
      pattern: "MIGRATIONS\\.md"
    - from: supabase/MIGRATIONS.md
      to: supabase/MIGRATION_MANIFEST.md
      via: "Related docs cross-link"
      pattern: "MIGRATION_MANIFEST"
---

<objective>
Hoàn thiện tài liệu migrations tại root `supabase/` — manifest đầy đủ, paths canonical, brownfield chain preserved.

Purpose: Operators và Claude executor biết chính xác apply path, overlap history, và inventory buckets/RPCs mà không squash production chain.
Output: `MIGRATIONS.md`, `MIGRATION_MANIFEST.md`, `STORAGE_GUIDE.md`, README cross-links cập nhật.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-supabase-migrations-optimize/07-CONTEXT.md
@.planning/phases/07-supabase-migrations-optimize/07-VERIFICATION.md
@supabase/MIGRATIONS.md
@supabase/MIGRATION_MANIFEST.md
@supabase/STORAGE_GUIDE.md

**Brownfield state (2026-06-14):** Docs + manifest + 035 file đã tồn tại từ autonomous run. Plan này **verify + gap-fill** — không recreate từ đầu nếu artifact đã đúng.

**Locked decisions:**
- D-01: `MIGRATIONS.md` trỏ root paths
- D-02: `MIGRATION_MANIFEST.md` per-file inventory + overlap notes
- D-04: `000_baseline.sql` deferred — document only in manifest

**Deferred (do NOT implement):** Full squash baseline, RLS rewrite.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize MIGRATIONS.md (D-01, MIG-01, MIG-06)</name>
  <files>supabase/MIGRATIONS.md</files>
  <action>
Audit `supabase/MIGRATIONS.md` against current repo layout (post-Phase 6 flat root).

Ensure ALL of:
1. Canonical location: `supabase/migrations/` at repo root (001–035) — zero references to `apps/web/`, `apps/admin/`, or `pnpm`.
2. Apply sections cover **both** paths: Supabase CLI (`supabase link` + `supabase db push` from repo root) AND Dashboard SQL Editor (apply only unapplied files in order).
3. Brownfield rule explicit per D-03: never rename/reorder applied migration files; additive only.
4. Verification commands use root scripts: `npm run check-types && npm run build` (not turbo/pnpm).
5. Cross-link to `MIGRATION_MANIFEST.md` and `STORAGE_GUIDE.md`.

If file already satisfies checks, make only minimal fixes. Do NOT add baseline squash instructions beyond "deferred — see manifest".
  </action>
  <verify>
    <automated>rg -l "apps/(web|admin)" supabase/MIGRATIONS.md; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }</automated>
  </verify>
  <done>MIGRATIONS.md documents root `supabase/migrations/`, apply paths, brownfield preservation — no stale monorepo paths</done>
</task>

<task type="auto">
  <name>Task 2: Enrich MIGRATION_MANIFEST inventory (D-02, D-04, MIG-02–04, MIG-07–08)</name>
  <files>supabase/MIGRATION_MANIFEST.md</files>
  <action>
Expand `supabase/MIGRATION_MANIFEST.md` to satisfy ROADMAP Phase 7 success criteria. Idempotent: merge missing sections into existing tables — do not delete accurate content.

**Per-file table:** Confirm rows for 001–035 exist with one-line purpose each (035 = public browse + CRM queue indexes).

**Overlap notes (MIG-03, MIG-04):** Keep and verify:
- RLS: `005` + `009` — performance patch; keep both on applied DBs
- RPC chain: `014` → `020` → `025` — `create or replace`; safe additive chain

**Storage buckets table (MIG-07):** Add dedicated section:

| Bucket | Migration | Public | Purpose |
|--------|-----------|--------|---------|
| marketplace-images | 008 | yes | Marketplace item images |
| crm-attachments | 011 | no | CRM file attachments |
| consign-submissions | 033 | no | Public consign photo uploads |

**RPC functions table (MIG-08):** Add dedicated section listing public functions from migrations:

| Function | Introduced/extended | Purpose |
|----------|---------------------|---------|
| handle_updated_at | 001, 030 | updated_at trigger helper |
| handle_new_user | 001, 015, 030 | Auth signup profile hook |
| log_audit_event | 007 | Audit log trigger |
| set_marketplace_state_timestamps | 002 | Lifecycle timestamp trigger |
| crm_dashboard_summary | 014 | Dashboard KPIs |
| crm_activity_series | 014 | Activity chart data |
| crm_funnel_summary | 020 | Funnel metrics |
| crm_lead_source_performance | 020 | Lead source report |
| crm_deal_aging | 020 | Deal aging report |
| crm_stage_velocity | 025 | Stage velocity |
| crm_funnel_by_source | 025 | Funnel by source |
| admin_upsert_profile | 030 | Admin profile upsert |

**Fresh install baseline (D-04):** Dedicated subsection stating `000_baseline.sql` is **deferred to Phase 8** — new projects apply full chain 001–035 until baseline published. Do NOT create `000_baseline.sql` in this phase.
  </action>
  <verify>
    <automated>rg -c "marketplace-images|crm-attachments|consign-submissions" supabase/MIGRATION_MANIFEST.md | ForEach-Object { if ([int]($_ -split ':')[1] -ge 3) { exit 0 } }; exit 1</automated>
  </verify>
  <done>Manifest has 001–035 inventory, overlap notes, bucket table, RPC table, and Phase 8 baseline deferral</done>
</task>

<task type="auto">
  <name>Task 3: Fix stale paths in STORAGE_GUIDE + README link (D-01)</name>
  <files>supabase/STORAGE_GUIDE.md, README.md</files>
  <action>
**STORAGE_GUIDE.md:** Replace ALL stale paths:
- `cd apps/admin` → run from repo root
- `apps/admin/supabase/migrations/` → `supabase/migrations/`
- `npx supabase db push` examples use repo root context
- Reference `supabase/MIGRATIONS.md` for full apply workflow

**README.md:** Add concise **Database / Supabase** subsection under Quick Start or Project Structure (do NOT rewrite entire outdated structure tree — minimal additive edit only):
- Canonical migrations: `supabase/migrations/`
- Docs: link to `supabase/MIGRATIONS.md`, `supabase/MIGRATION_MANIFEST.md`, `supabase/STORAGE_GUIDE.md`
- Apply note: brownfield keeps incremental chain; see manifest for inventory

Keep edit scoped — this phase optimizes migration docs, not full README modernization.
  </action>
  <verify>
    <automated>rg -l "apps/(web|admin)" supabase/STORAGE_GUIDE.md; if ($LASTEXITCODE -eq 0) { exit 1 } else { rg -q "MIGRATIONS.md" README.md; if ($LASTEXITCODE -ne 0) { exit 1 } else { exit 0 } }</automated>
  </verify>
  <done>STORAGE_GUIDE uses root paths; README links to migration docs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs→operator | Migration docs guide destructive DDL apply on production DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | I | MIGRATIONS.md apply instructions | mitigate | Explicit brownfield rule: never rename applied files; additive 035 only per D-03 |
| T-07-02 | I | Manifest overlap notes | mitigate | Document 005/009 and RPC chain so operators do not manually squash |
| T-07-03 | S | STORAGE_GUIDE service-role examples | accept | Existing pattern; service role server-only — no change in this plan |
</threat_model>

<verification>
```bash
rg "apps/(web|admin)" supabase/MIGRATIONS.md supabase/STORAGE_GUIDE.md
# Expected: no matches

rg "035|marketplace-images|crm_dashboard_summary" supabase/MIGRATION_MANIFEST.md
# Expected: matches present
```
</verification>

<success_criteria>
1. Zero `apps/web` or `apps/admin` paths in `supabase/MIGRATIONS.md` and `supabase/STORAGE_GUIDE.md`
2. Manifest documents all 35 files, 3 buckets, 12 RPCs, overlap notes, baseline deferred
3. README links to migration docs at root `supabase/`
</success_criteria>

<output>
After completion, create `.planning/phases/07-supabase-migrations-optimize/07-01-SUMMARY.md`
</output>

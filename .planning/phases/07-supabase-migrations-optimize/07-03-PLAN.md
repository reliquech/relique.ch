---
phase: 07-supabase-migrations-optimize
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - .planning/phases/07-supabase-migrations-optimize/07-VERIFICATION.md
autonomous: false
requirements:
  - MIG-05
  - MIG-06
must_haves:
  truths:
    - "Migration 035 is applied on linked Supabase project (remote schema has three new indexes)"
    - "npm run check-types and npm run build pass after apply"
    - "Types regen explicitly deferred to Phase 4 per D-07 — documented, not executed"
  artifacts:
    - path: .planning/phases/07-supabase-migrations-optimize/07-VERIFICATION.md
      provides: "Phase gate with apply status"
      contains: "035"
  key_links:
    - from: supabase db push
      to: supabase/migrations/035_optimize_public_browse_indexes.sql
      via: "Supabase migration history"
      pattern: "035_optimize_public_browse_indexes"
    - from: remote PostgreSQL
      to: marketplace_items_public_browse_idx
      via: "pg_indexes or SQL Editor verify"
      pattern: "marketplace_items_public_browse_idx"
---

<objective>
Apply migration 035 lên Supabase remote và đóng phase gate — brownfield chain applied, build green.

Purpose: Indexes tồn tại trên production/staging DB; phase không kết thúc chỉ với file SQL local.
Output: 035 applied remotely, VERIFICATION.md updated, D-07 deferral documented.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-supabase-migrations-optimize/07-CONTEXT.md
@.planning/phases/07-supabase-migrations-optimize/07-VERIFICATION.md
@supabase/MIGRATIONS.md
@supabase/migrations/035_optimize_public_browse_indexes.sql

**Current blocker (STATE.md):** Migration 035 chưa apply remote — code complete only.

**Locked decisions:**
- D-03: Apply 035 additively via `supabase db push` or SQL Editor — never squash
- D-07: Regen `src/lib/supabase/types.ts` **deferred to Phase 4** — do NOT run `supabase gen types` in this plan unless user explicitly overrides during checkpoint

**Prerequisites:** Plan 07-02 complete (035 file verified, 35-file chain).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-apply check — skip if 035 already applied</name>
  <files>.planning/phases/07-supabase-migrations-optimize/07-VERIFICATION.md</files>
  <action>
Before attempting apply, detect whether 035 is already on the linked remote:

**Option A — Supabase CLI (preferred):**
```bash
supabase migration list
```
Look for `035_optimize_public_browse_indexes` as applied.

**Option B — SQL check (if linked):**
```sql
SELECT indexname FROM pg_indexes
WHERE schemaname = 'public'
  AND indexname IN (
    'marketplace_items_public_browse_idx',
    'leads_source_status_idx',
    'consigned_items_queue_idx'
  );
```
Expect 3 rows if fully applied.

If all 3 indexes exist OR migration list shows 035 applied → record "035 already applied" in working notes and **skip Task 2 apply** (proceed to Task 3 gate only).

If `supabase` CLI not installed, not linked, or credentials missing → record blocker reason; proceed to Task 2 which will escalate to human checkpoint.
  </action>
  <verify>
    <automated>Test-Path supabase/migrations/035_optimize_public_browse_indexes.sql; exit $LASTEXITCODE</automated>
  </verify>
  <done>Apply status determined — either already applied or ready for Task 2 push</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>[BLOCKING] Apply migration 035 via supabase db push</name>
  <files>supabase/migrations/035_optimize_public_browse_indexes.sql, .planning/phases/07-supabase-migrations-optimize/07-VERIFICATION.md</files>
  <action>
Apply `035_optimize_public_browse_indexes.sql` to the linked Supabase project.

**Automated path (executor tries first):**
```bash
# From repo root — requires prior: supabase login + supabase link --project-ref <ref>
supabase db push
```

**If CLI fails** (not linked, no auth, no config.toml):
1. Document exact failure in VERIFICATION.md
2. Pause at this checkpoint for human to either:
   - Run `supabase link --project-ref &lt;ref&gt;` + `supabase db push` locally, OR
   - Paste `supabase/migrations/035_optimize_public_browse_indexes.sql` into Supabase Dashboard → SQL Editor → Run

**Post-apply verify (required):**
```sql
SELECT indexname FROM pg_indexes
WHERE indexname IN (
  'marketplace_items_public_browse_idx',
  'leads_source_status_idx',
  'consigned_items_queue_idx'
);
```
Expect **3 rows**.

Per D-03: apply **only** 035 if 001–034 already applied. Do NOT squash or re-run full chain on brownfield DB.
  </action>
  <verify>
    <automated>supabase migration list 2>$null | Select-String "035_optimize"; if ($LASTEXITCODE -ne 0) { Write-Host "MISSING — human must apply via db push or SQL Editor"; exit 0 }</automated>
  </verify>
  <how-to-verify>
1. Supabase Dashboard → Database → Migrations (or `supabase migration list`) shows `035_optimize_public_browse_indexes` applied
2. SQL Editor: index query returns 3 rows
3. Type "applied" or "approved" to resume executor for Task 3
  </how-to-verify>
  <resume-signal>Type "applied" after db push or SQL Editor run succeeds, or describe blocker</resume-signal>
  <done>Remote DB has all three 035 indexes; migration history records 035</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — build + D-07 deferral + VERIFICATION update</name>
  <files>.planning/phases/07-supabase-migrations-optimize/07-VERIFICATION.md</files>
  <action>
Run phase gate after 035 applied (or confirmed already applied):

1. **Build gate:**
   ```bash
   npm run check-types && npm run build
   ```

2. **D-07 deferral (do NOT regen types):** Update `07-VERIFICATION.md`:
   - Mark apply 035: PASS (with date + method: db push or SQL Editor)
   - Mark types regen: **DEFERRED to Phase 4** per D-07 — note `supabase gen types typescript --project-id &lt;ref&gt; > src/lib/supabase/types.ts` as Phase 4 action
   - Mark baseline squash: DEFERRED to Phase 8 per D-04
   - Set phase status: `complete` if all automated gates pass + 035 applied

3. **Do NOT** modify `src/lib/supabase/types.ts` in this task.

4. Record final score vs ROADMAP success criteria (8 items; types item = deferred not failed).
  </action>
  <verify>
    <automated>npm run check-types; if ($LASTEXITCODE -ne 0) { exit 1 }; npm run build; exit $LASTEXITCODE</automated>
  </verify>
  <done>Build passes; VERIFICATION.md reflects 035 applied + D-07/D-04 deferrals; phase gate closed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI→remote Supabase | Service credentials apply DDL to hosted PostgreSQL |
| operator→SQL Editor | Manual DDL paste bypasses CLI audit trail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | E | supabase db push | mitigate | Pre-check migration list; apply only unapplied 035; IF NOT EXISTS in DDL |
| T-07-08 | I | SQL Editor manual apply | mitigate | Use exact file content from repo; verify 3 indexes post-apply |
| T-07-09 | S | SUPABASE_SERVICE_ROLE_KEY in CLI session | transfer | Credentials from env/.env.local; never commit; human links project |
| T-07-10 | D | Partial apply (1 of 3 indexes) | mitigate | Post-apply SQL verify requires all 3 index names |
</threat_model>

<verification>
```bash
npm run check-types && npm run build
# Expected: exit 0

# Remote (after apply):
# SELECT count(*) FROM pg_indexes WHERE indexname IN (...);  → 3
```
</verification>

<success_criteria>
1. Migration 035 applied on remote Supabase project
2. Three indexes verifiable via `pg_indexes` query
3. `npm run check-types` + `npm run build` pass
4. `07-VERIFICATION.md` updated — types regen deferred to Phase 4, baseline deferred to Phase 8
</success_criteria>

<output>
After completion, create `.planning/phases/07-supabase-migrations-optimize/07-03-SUMMARY.md`
</output>

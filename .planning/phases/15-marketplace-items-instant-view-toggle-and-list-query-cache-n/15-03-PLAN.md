---
phase: 15-marketplace-items-instant-view-toggle-and-list-query-cache-n
plan: 03
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - src/components/admin/marketplace/pages/ItemsPage.tsx
  - src/components/admin/marketplace/items/MarketplaceItemsToolbar.tsx
autonomous: true
requirements:
  - CACHE-15-01
  - CACHE-15-04
must_haves:
  truths:
    - "Table/Grid toggle does not call refetch"
    - "Mutation success forces active list refresh and invalidates list cache"
    - "Retry button forces network refetch"
    - "Background refresh has subtle non-blocking UI feedback"
---

<objective>
Wire cached query behavior into the Marketplace Items page without changing table/grid visual contracts.
</objective>

<threat_model>
T15-05: Mutations can leave cached list states stale. After successful mutations, invalidate all list cache entries and force refresh the active query.
T15-06: Non-blocking refresh indicators can introduce layout shift. Use fixed small text in existing toolbar/count area or no indicator; do not add a new large banner.
</threat_model>

<tasks>
<task id="15-03-01" type="auto">
  <name>Force refresh after mutations and retry</name>
  <files>src/components/admin/marketplace/pages/ItemsPage.tsx</files>
  <read_first>
    - src/components/admin/marketplace/pages/ItemsPage.tsx
    - src/features/marketplace/hooks/useMarketplaceItemsQuery.ts
    - src/features/marketplace/services/marketplaceService.ts
  </read_first>
  <action>
Update `ItemsPage` destructuring from `useMarketplaceItemsQuery(state)` to include `refreshing`, `isStale`, and `invalidateCache`.
After successful bulk, duplicate, publish, unpublish, restore, archive, and delete actions:
- call `invalidateCache()`
- call `refetch({ force: true })`

Update the error Retry button to call `refetch({ force: true })`.
Do not call `refetch` from `onViewChange`; view toggle should remain `onViewChange={setView}` only.
  </action>
  <acceptance_criteria>
    - `ItemsPage.tsx` contains `invalidateCache`
    - `ItemsPage.tsx` contains `refetch({ force: true })`
    - `ItemsPage.tsx` still contains `onViewChange={setView}`
    - `ItemsPage.tsx` does not contain `onViewChange={(`
    - `npx eslint --max-warnings 0 src/components/admin/marketplace/pages/ItemsPage.tsx` exits 0
  </acceptance_criteria>
</task>

<task id="15-03-02" type="auto">
  <name>Add non-blocking refresh signal</name>
  <files>
    src/components/admin/marketplace/pages/ItemsPage.tsx
    src/components/admin/marketplace/items/MarketplaceItemsToolbar.tsx
  </files>
  <read_first>
    - src/components/admin/marketplace/pages/ItemsPage.tsx
    - src/components/admin/marketplace/items/MarketplaceItemsToolbar.tsx
    - src/components/admin/marketplace/items/MarketplaceItemsCountSummary.tsx
  </read_first>
  <action>
Add optional props to `MarketplaceItemsToolbar`:
- `refreshing?: boolean`
- `isStale?: boolean`

Render a compact text indicator near the count summary:
- if `refreshing`: `Refreshing`
- else if `isStale`: `Cached`
- else render nothing.

In `ItemsPage`, pass `refreshing={refreshing}` and `isStale={isStale}`.
Do not alter table/grid layout dimensions.
  </action>
  <acceptance_criteria>
    - `MarketplaceItemsToolbar.tsx` contains `refreshing?: boolean`
    - `MarketplaceItemsToolbar.tsx` contains `isStale?: boolean`
    - `MarketplaceItemsToolbar.tsx` contains `Refreshing`
    - `MarketplaceItemsToolbar.tsx` contains `Cached`
    - `npx eslint --max-warnings 0 src/components/admin/marketplace/pages/ItemsPage.tsx src/components/admin/marketplace/items/MarketplaceItemsToolbar.tsx` exits 0
  </acceptance_criteria>
</task>
</tasks>

<verification>
- `npx eslint --max-warnings 0 src/components/admin/marketplace/pages/ItemsPage.tsx src/components/admin/marketplace/items/MarketplaceItemsToolbar.tsx`
- `npm run check-types`
- Manual browser network check: table/grid switch triggers no `/api/marketplace` request.
</verification>

<success_criteria>
- View toggles are instant and data-fetch independent.
- Mutation paths force fresh active list data.
- Cached/background refresh state is visible without layout disruption.
</success_criteria>


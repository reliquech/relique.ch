# Copy to `.env.local` for local development (never commit secrets).

# Site
NEXT_PUBLIC_SITE_URL=http://localhost:1300

# Supabase — production MUST use HTTPS (e.g. Cloudflare tunnel)
# NEXT_PUBLIC_SUPABASE_URL=https://relique-supabase.minhmice.com
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Vercel production checklist:
# - Project Settings → Root Directory: leave EMPTY (repo root). Do NOT use apps/web (removed in Phase 6).
# - Install Command: npm install | Build Command: npm run build (or use vercel.json at repo root)
# - NEXT_PUBLIC_SUPABASE_URL=https://relique-supabase.minhmice.com (not http://)
# - NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon>
# - SUPABASE_SERVICE_ROLE_KEY=<service role, server only>
# - NEXT_PUBLIC_SITE_URL=https://relique.ch
# - Verify tunnel: curl https://relique-supabase.minhmice.com/auth/v1/health
